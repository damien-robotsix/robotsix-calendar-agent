{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"robotsix-calendar-agent","text":"<p>An agent-comm agent that manages a Radicale server's calendars (CalDAV) and contacts (CardDAV) \u2014 full read-write including delete \u2014 driven entirely by the <code>robotsix-agent-comm</code> messaging system.</p>"},{"location":"#architecture","title":"Architecture","text":"<pre><code>agent-comm Request \u2192 CalendarAgent \u2192 IntentParser (llmio)\n                                   \u2192 CalDavClient (caldav) \u2192 Radicale\n                                   \u2190 Response / Error\n</code></pre> <ol> <li>A natural-language instruction arrives as an agent-comm <code>Request</code>.</li> <li><code>CalendarAgent</code> passes the instruction to <code>IntentParser</code>, which uses    <code>robotsix-llmio</code> to classify it into one of 8 operations and extract    structured parameters.</li> <li>The parsed intent is dispatched to <code>CalDavClient</code>, which wraps the    <code>caldav</code> library to perform CRUD operations against the Radicale server.</li> <li>The result is returned as a correlated <code>Response</code> (or <code>Error</code> on failure).</li> </ol>"},{"location":"#getting-started","title":"Getting started","text":""},{"location":"#1-configure-radicale-access","title":"1. Configure Radicale access","text":"<p>Set the environment variables pointing to your Radicale server:</p> <pre><code>export RADICALE_URL=\"https://radicale.example.com\"\nexport RADICALE_USERNAME=\"your-username\"\nexport RADICALE_PASSWORD=\"your-password\"  # pragma: allowlist secret\n</code></pre> <p>Alternatively, pass these values as constructor arguments to <code>CalendarAgent</code>.</p>"},{"location":"#2-install-dependencies","title":"2. Install dependencies","text":"<pre><code>uv sync\n</code></pre>"},{"location":"#3-start-the-agent","title":"3. Start the agent","text":"<pre><code>from robotsix_calendar_agent import CalendarAgent\n\nagent = CalendarAgent()\nagent.start()\n</code></pre>"},{"location":"#4-send-a-request-via-agent-comm","title":"4. Send a request via agent-comm","text":"<pre><code>from robotsix_agent_comm.sdk import Agent\nfrom robotsix_agent_comm.transport import Registry\nfrom robotsix_agent_comm.protocol import Request, Metadata\n\nregistry = Registry()\n# The calendar agent has already registered itself on this registry.\n# Create a requester agent to send a message:\nrequester = Agent(\"requester\", registry)\nrequester.start()\n\n# Send a request to the calendar agent:\nresponse = requester.send_request(\n    \"calendar\",\n    {\"instruction\": \"list events this week\"},\n)\nprint(response.body)\n</code></pre>"},{"location":"#deployment","title":"Deployment","text":"<p>The agent supports two transport modes, selected by the <code>CALENDAR_AGENT_TRANSPORT</code> environment variable.</p>"},{"location":"#in-process-transport-inprocess-default","title":"In-process transport (<code>inprocess</code>, default)","text":"<p>The agent constructs its own in-memory <code>robotsix_agent_comm.transport.Registry</code>. This is the zero-config path used by tests and single-process deployments where a requester and the calendar agent live in the same process:</p> <pre><code>from robotsix_calendar_agent import CalendarAgent\n\nagent = CalendarAgent()  # transport=None \u2192 in-process Registry\nagent.start()\n</code></pre>"},{"location":"#brokered-transport-brokered","title":"Brokered transport (<code>brokered</code>)","text":"<p>The agent connects to a secured, TLS-authenticated broker and runs as an independent long-lived service. The broker pushes dispatched messages to the agent's handler. Connection details come from the <code>BROKER_*</code> environment variables (see the configuration reference below).</p> <p>A console-script entrypoint, <code>calendar-agent</code>, drives the long-lived service. It reads <code>CALENDAR_AGENT_TRANSPORT</code>, builds the selected transport, starts the agent, and blocks until <code>SIGTERM</code>/<code>SIGINT</code>, on which it stops the agent and exits cleanly:</p> <pre><code>export CALENDAR_AGENT_TRANSPORT=brokered\nexport BROKER_HOST=broker.example.com\nexport BROKER_TLS_CA=/certs/ca.pem\nexport BROKER_AGENT_TOKEN=your-broker-token\ncalendar-agent\n</code></pre>"},{"location":"#docker-compose","title":"Docker / Compose","text":"<p>A <code>Dockerfile</code> at the repo root packages the agent (defaulting to <code>CALENDAR_AGENT_TRANSPORT=brokered</code>) and runs the <code>calendar-agent</code> console-script:</p> <pre><code>docker build -t calendar-agent .\n</code></pre> <p>A <code>docker-compose.yml</code> defines the <code>calendar-agent</code> service alongside a <code>broker</code> service (the broker's real definition lives in the <code>robotsix-agent-comm</code> repo). TLS material is mounted read-only and all broker environment variables are wired in; the service restarts <code>unless-stopped</code>:</p> <pre><code>docker compose up calendar-agent\n</code></pre>"},{"location":"#operations-reference","title":"Operations reference","text":"Operation Example instruction Key params <code>list_events</code> \"list events this week\" <code>start</code>, <code>end</code> (ISO 8601) <code>create_event</code> \"add a dentist appointment next Tuesday at 3pm\" <code>summary</code>, <code>dtstart</code>, <code>dtend</code> <code>update_event</code> \"reschedule the dentist to 4pm\" <code>uid</code>, updated fields <code>delete_event</code> \"cancel the dentist appointment\" <code>uid</code> <code>list_contacts</code> \"show all contacts\" (none) <code>create_contact</code> \"add John Doe, john@example.com\" <code>full_name</code>, <code>email</code>, <code>phone</code> <code>update_contact</code> \"change John's email to john.doe@example.com\" <code>uid</code>, updated fields <code>delete_contact</code> \"remove John Doe from contacts\" <code>uid</code>"},{"location":"#configuration-reference","title":"Configuration reference","text":"<p>See Configuration for the canonical environment-variable reference.</p>"},{"location":"#environment-variables","title":"Environment variables","text":"Variable Required Default Description <code>RADICALE_URL</code> Yes \u2014 Radicale server URL <code>RADICALE_USERNAME</code> Yes \u2014 Radicale username <code>RADICALE_PASSWORD</code> Yes \u2014 Radicale password <code>CALENDAR_AGENT_TRANSPORT</code> No <code>inprocess</code> Transport mode: <code>inprocess</code> (in-memory <code>Registry</code>) or <code>brokered</code> <code>CALENDAR_AGENT_ID</code> No <code>robotsix-calendar</code> Agent identity registered on the broker <code>BROKER_HOST</code> When brokered \u2014 Broker hostname/IP <code>BROKER_PORT</code> No <code>9090</code> Broker port <code>BROKER_SCHEME</code> When brokered <code>https</code> Broker URL scheme (<code>http</code> or <code>https</code>) <code>BROKER_TLS_CA</code> When brokered \u2014 Path to CA certificate PEM for verifying the broker <code>BROKER_CLIENT_CERT</code> No \u2014 Path to client certificate PEM (mTLS, optional) <code>BROKER_CLIENT_KEY</code> No \u2014 Path to client key PEM (mTLS, optional) <code>BROKER_AGENT_TOKEN</code> When brokered \u2014 Authentication token for the broker"},{"location":"#constructor-options-calendaragent","title":"Constructor options (<code>CalendarAgent</code>)","text":"Parameter Type Default Description <code>agent_id</code> <code>str</code> <code>\"calendar\"</code> Agent-comm agent ID <code>radicale_url</code> <code>str \\| None</code> <code>None</code> Radicale URL (falls back to env) <code>radicale_username</code> <code>str \\| None</code> <code>None</code> Radicale username (falls back to env) <code>radicale_password</code> <code>str \\| None</code> <code>None</code> Radicale password (falls back to env) <code>llm_model_config</code> <code>dict \\| None</code> <code>None</code> Forwarded to llmio for model selection"},{"location":"configuration/","title":"Configuration","text":"<p>All configuration is read from environment variables via <code>pydantic_settings.BaseSettings</code>. The settings class lives at <code>src/robotsix_calendar_agent/settings.py</code>.</p>"},{"location":"configuration/#environment-variables","title":"Environment variables","text":""},{"location":"configuration/#core-settings","title":"Core settings","text":"Variable Type Default Description <code>RADICALE_URL</code> <code>str</code> <code>\"\"</code> Radicale server URL <code>RADICALE_USERNAME</code> <code>str</code> <code>\"\"</code> Radicale username <code>RADICALE_PASSWORD</code> <code>SecretStr</code> <code>SecretStr(\"\")</code> Radicale password <code>RADICALE_DEFAULT_CALENDAR</code> <code>str</code> <code>\"Robotsix\"</code> Default calendar name for write operations when no <code>calendar_id</code> is provided <code>CALENDAR_AGENT_TRANSPORT</code> <code>str</code> <code>\"inprocess\"</code> Transport mode: <code>inprocess</code> (in-memory <code>Registry</code>) or <code>brokered</code> <code>CALENDAR_AGENT_ID</code> <code>str</code> <code>\"robotsix-calendar\"</code> Agent identity registered on the broker <code>BROKER_HOST</code> <code>str</code> <code>\"\"</code> Broker hostname or IP address <code>BROKER_PORT</code> <code>int</code> <code>9090</code> Broker port (1\u201365535) <code>BROKER_SCHEME</code> <code>str</code> <code>\"https\"</code> Broker URL scheme (<code>http</code> or <code>https</code>) <code>BROKER_AGENT_TOKEN</code> <code>SecretStr</code> <code>SecretStr(\"\")</code> Authentication token for the broker <code>BROKER_TLS_CA</code> <code>str \\| None</code> <code>None</code> Path to CA certificate PEM for verifying the broker <code>BROKER_CLIENT_CERT</code> <code>str \\| None</code> <code>None</code> Path to client certificate PEM for mTLS (optional) <code>BROKER_CLIENT_KEY</code> <code>str \\| None</code> <code>None</code> Path to client key PEM for mTLS (optional)"},{"location":"configuration/#component-agent-settings","title":"Component-agent settings","text":"<p>The component-agent responder adds <code>monitor</code>, <code>config-get</code>, and <code>config-set</code> management kinds to the running agent.  It is disabled by default and gated behind two conditions:</p> <ol> <li>The <code>robotsix-agent-comm</code> SDK must be installed (it is a core    dependency).</li> <li><code>COMPONENT_AGENT_ENABLED</code> must be <code>true</code> and a non-empty    <code>COMPONENT_AGENT_TOKEN</code> must be configured.</li> </ol> Variable Type Default Description <code>COMPONENT_AGENT_ENABLED</code> <code>bool</code> <code>False</code> Enable the component-agent responder <code>COMPONENT_AGENT_TOKEN</code> <code>SecretStr</code> <code>SecretStr(\"\")</code> Bearer token shared with the broker auth scheme <code>COMPONENT_AGENT_ID</code> <code>str</code> <code>\"robotsix-calendar\"</code> Agent identity (must match the broker registration) <p>When <code>COMPONENT_AGENT_ENABLED=true</code> and the token is empty, the process refuses to start with a <code>ValueError</code> mentioning <code>COMPONENT_AGENT_TOKEN</code>.</p>"},{"location":"configuration/#runtime-configurable-keys","title":"Runtime-configurable keys","text":"<p>Only a subset of keys can be changed at runtime via <code>config-set</code>:</p> Key Type Rationale <code>radicale_default_calendar</code> <code>str</code> Safe to change; updates the live <code>CalDavClient</code> default calendar <p>All other keys are startup-only \u2014 changing them mid-flight would either have no effect or corrupt the running agent (e.g. CalDAV identity, broker connection, transport mode).</p>"},{"location":"configuration/#management-kinds","title":"Management kinds","text":"<p>When the component-agent responder is active, the agent answers three additional request kinds on the existing broker connection:</p>"},{"location":"configuration/#monitor","title":"<code>monitor</code>","text":"<p>Returns genuine live telemetry:</p> <ul> <li><code>agent_id</code>, <code>uptime_seconds</code>, <code>request_count</code>, <code>error_count</code>,   <code>in_flight</code>, <code>last_request_ts</code></li> <li><code>caldav_url</code>, <code>default_calendar</code></li> <li><code>caldav_health</code> \u2014 a live CalDAV reachability probe   (<code>connected</code> + <code>calendar_count</code>)</li> <li><code>capabilities</code> \u2014 the list of supported management kinds</li> </ul>"},{"location":"configuration/#config-get","title":"<code>config-get</code>","text":"<p>Returns a redacted snapshot of all configuration (secret values replaced with <code>\"***\"</code>) and per-key descriptors (type, whether settable, whether secret).</p>"},{"location":"configuration/#config-set","title":"<code>config-set</code>","text":"<p>Validates then applies a set of config updates.  The request body must contain an <code>updates</code> dict mapping dotted-path keys (e.g. <code>\"radicale_default_calendar\"</code>) to new values.</p> <ul> <li>Rejects unknown keys, non-settable keys, and invalid values   with a broker <code>Error</code> (no mutation occurs).</li> <li>On success returns an audit map <code>{key: (old, new)}</code> with   secrets redacted, and the change is logged.</li> </ul>"},{"location":"configuration/#redaction","title":"Redaction","text":"<p>All secret fields (<code>radicale_password</code>, <code>broker_agent_token</code>, <code>component_agent_token</code>) are replaced with the sentinel <code>\"***\"</code> in every read path \u2014 <code>config-get</code>, <code>describe_config</code>, and the audit returned by <code>config-set</code>.  Real secret values are never exposed through the management API.</p>"},{"location":"reference/","title":"robotsix_calendar_agent","text":""},{"location":"reference/#robotsix_calendar_agent","title":"<code>robotsix_calendar_agent</code>","text":"<p>robotsix_calendar_agent \u2014 agent-comm calendar and contacts agent for Radicale.</p> <p>This package provides a calendar and contacts management agent driven by the <code>robotsix-agent-comm</code> messaging system.  It parses natural-language instructions via <code>robotsix-llmio</code> and executes CalDAV/CardDAV operations against a Radicale server.</p>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient","title":"<code>CalDavClient(url: str, username: str, password: str, default_calendar: str = '')</code>","text":"<p>Typed wrapper around <code>caldav.DAVClient</code> for Radicale.</p> <p>All operations convert caldav/requests exceptions to :class:<code>OperationError</code> with appropriate codes.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>Radicale server URL.</p> required <code>username</code> <code>str</code> <p>Radicale username.</p> required <code>password</code> <code>str</code> <p>Radicale password.</p> required <p>Raises:</p> Type Description <code>OperationError</code> <p>If authentication fails.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>def __init__(\n    self, url: str, username: str, password: str, default_calendar: str = \"\"\n) -&gt; None:\n    # ``caldav`` ships incomplete type information (mypy resolves the\n    # module to ``object``), so route it through ``Any`` to keep the\n    # wrapper strict-clean without per-call ignores.\n    import caldav\n\n    _caldav: Any = caldav\n\n    self._url = url\n    self._default_calendar = default_calendar\n    try:\n        self._client = _caldav.DAVClient(\n            url=url, username=username, password=password\n        )\n        self._principal = self._client.principal()\n        logger.info(\"CalDavClient connected to %s as %s\", url, username)\n    except _caldav.error.AuthorizationError as exc:\n        logger.exception(\"CalDAV auth failed for %s as %s: %s\", url, username, exc)\n        raise OperationError(\n            code=\"auth_failed\",\n            message=f\"Authentication failed: {exc}\",\n        ) from exc\n    except Exception as exc:\n        logger.exception(\"Failed to connect to Radicale at %s: %s\", url, exc)\n        raise OperationError(\n            code=\"caldav_error\",\n            message=f\"Failed to connect to Radicale: {exc}\",\n        ) from exc\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient.create_contact","title":"<code>create_contact(contact: Contact, addressbook_id: str = '') -&gt; Contact</code>","text":"<p>Create a contact; return the contact with server-assigned uid.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"create contact\")\ndef create_contact(self, contact: Contact, addressbook_id: str = \"\") -&gt; Contact:\n    \"\"\"Create a contact; return the contact with server-assigned uid.\"\"\"\n    logger.debug(\n        \"create_contact uid=%r addressbook_id=%r full_name=%r\",\n        contact.uid,\n        addressbook_id,\n        contact.full_name,\n    )\n    import uuid\n\n    if not contact.uid:\n        contact = Contact(\n            uid=str(uuid.uuid4()),\n            full_name=contact.full_name,\n            email=contact.email,\n            phone=contact.phone,\n            address=contact.address,\n            addressbook_id=contact.addressbook_id,\n        )\n    ab = self._get_addressbook(addressbook_id)\n    vcard = self._contact_to_vcard(contact)\n    saved = ab.save_object(vcard)\n    return self._to_contact(saved, addressbook_id=ab.name)\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient.create_event","title":"<code>create_event(event: CalendarEvent, calendar_id: str = '') -&gt; CalendarEvent</code>","text":"<p>Create an event; return the event with its server-assigned uid.</p> <p>If calendar_id is empty, use the default calendar.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"create event\")\ndef create_event(\n    self, event: CalendarEvent, calendar_id: str = \"\"\n) -&gt; CalendarEvent:\n    \"\"\"Create an event; return the event with its server-assigned uid.\n\n    If *calendar_id* is empty, use the default calendar.\n    \"\"\"\n    logger.debug(\n        \"create_event uid=%r calendar_id=%r summary=%r\",\n        event.uid,\n        calendar_id,\n        event.summary,\n    )\n    import uuid\n\n    if not event.uid:\n        # Generate a temporary UID; the server may replace it.\n        event = CalendarEvent(\n            uid=str(uuid.uuid4()),\n            summary=event.summary,\n            description=event.description,\n            location=event.location,\n            dtstart=event.dtstart,\n            dtend=event.dtend,\n            calendar_id=event.calendar_id,\n        )\n    cal = self._get_calendar(calendar_id)\n    ical = self._event_to_ical(event)\n    saved = cal.save_event(ical)\n    return self._to_calendar_event(saved, calendar_id=cal.name)\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient.delete_contact","title":"<code>delete_contact(uid: str, addressbook_id: str = '') -&gt; None</code>","text":"<p>Delete the contact identified by uid. Idempotent.</p> <p>Returns <code>None</code> when the UID does not exist (already deleted).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"delete contact\")\ndef delete_contact(self, uid: str, addressbook_id: str = \"\") -&gt; None:\n    \"\"\"Delete the contact identified by *uid*. Idempotent.\n\n    Returns ``None`` when the UID does not exist (already deleted).\n    \"\"\"\n    logger.debug(\"delete_contact uid=%r addressbook_id=%r\", uid, addressbook_id)\n    ab = self._get_addressbook(addressbook_id)\n    existing = ab.search(f\"UID:{uid}\")\n    if not existing:\n        return None\n    existing[0].delete()\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient.delete_event","title":"<code>delete_event(uid: str, calendar_id: str = '') -&gt; None</code>","text":"<p>Delete the event identified by uid. Idempotent.</p> <p>When calendar_id is empty, iterates all calendars to locate the UID.  Returns <code>None</code> when the UID does not exist in any calendar (already deleted).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"delete event\")\ndef delete_event(self, uid: str, calendar_id: str = \"\") -&gt; None:\n    \"\"\"Delete the event identified by *uid*. Idempotent.\n\n    When *calendar_id* is empty, iterates **all** calendars to locate\n    the UID.  Returns ``None`` when the UID does not exist in any\n    calendar (already deleted).\n    \"\"\"\n    logger.debug(\"delete_event uid=%r calendar_id=%r\", uid, calendar_id)\n    if calendar_id:\n        cal = self._get_calendar(calendar_id)\n        event_obj = cal.event(uid=uid)\n        if event_obj is None:\n            return None\n        event_obj.delete()\n    else:\n        for cal in self._iter_calendars(\"\"):\n            event_obj = cal.event(uid=uid)\n            if event_obj is not None:\n                event_obj.delete()\n                return None\n        return None\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient.health","title":"<code>health() -&gt; dict[str, Any]</code>","text":"<p>Perform a one-shot CalDAV reachability check.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p><code>{\"connected\": True, \"calendar_count\": int}</code> on success, or</p> <code>dict[str, Any]</code> <p><code>{\"connected\": False, \"error\": str}</code> when the probe fails.</p> <p>This is called on-demand by <code>monitor</code> \u2014 it does not cache stale state.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>def health(self) -&gt; dict[str, Any]:\n    \"\"\"Perform a one-shot CalDAV reachability check.\n\n    Returns:\n        ``{\"connected\": True, \"calendar_count\": int}`` on success, or\n        ``{\"connected\": False, \"error\": str}`` when the probe fails.\n\n    This is called on-demand by ``monitor`` \u2014 it does **not** cache\n    stale state.\n    \"\"\"\n    try:\n        calendars = self._principal.calendars()\n    except Exception as exc:\n        logger.warning(\"CalDAV health probe failed: %s\", exc)\n        return {\"connected\": False, \"error\": str(exc)}\n    else:\n        return {\"connected\": True, \"calendar_count\": len(calendars)}\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient.list_calendars","title":"<code>list_calendars() -&gt; list[str]</code>","text":"<p>Return the names of all available calendar collections.</p> <p>Only VEVENT / calendar collections are included (addressbooks come from the separate <code>self._principal.addressbooks()</code> and are inherently excluded).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list calendars\")\ndef list_calendars(self) -&gt; list[str]:\n    \"\"\"Return the names of all available calendar collections.\n\n    Only VEVENT / calendar collections are included (addressbooks\n    come from the separate ``self._principal.addressbooks()`` and\n    are inherently excluded).\n    \"\"\"\n    return [cal.name for cal in self._principal.calendars()]\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient.list_contacts","title":"<code>list_contacts(addressbook_id: str = '') -&gt; list[Contact]</code>","text":"<p>Return all contacts.</p> <p>If addressbook_id is empty, use the default address book.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list contacts\")\ndef list_contacts(self, addressbook_id: str = \"\") -&gt; list[Contact]:\n    \"\"\"Return all contacts.\n\n    If *addressbook_id* is empty, use the default address book.\n    \"\"\"\n    logger.debug(\"list_contacts addressbook_id=%r\", addressbook_id)\n    ab = self._get_addressbook(addressbook_id)\n    results = ab.search()\n    return [self._to_contact(r, addressbook_id=ab.name) for r in results]\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient.list_events","title":"<code>list_events(start: str, end: str, calendar_id: str = '') -&gt; list[CalendarEvent]</code>","text":"<p>Return events in the ISO 8601 date range.</p> <p>When calendar_id is empty, events are aggregated from all calendars.  Each event is tagged with its source <code>calendar_id</code>.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list events\")\ndef list_events(\n    self, start: str, end: str, calendar_id: str = \"\"\n) -&gt; list[CalendarEvent]:\n    \"\"\"Return events in the ISO 8601 date range.\n\n    When *calendar_id* is empty, events are aggregated from **all**\n    calendars.  Each event is tagged with its source ``calendar_id``.\n    \"\"\"\n    logger.debug(\n        \"list_events start=%r end=%r calendar_id=%r\",\n        start,\n        end,\n        calendar_id,\n    )\n    start_dt, end_dt = self._iso_date_range(start, end)\n    aggregated: list[CalendarEvent] = []\n    for cal in self._iter_calendars(calendar_id):\n        results = cal.search(\n            start=start_dt,\n            end=end_dt,\n            event=True,\n            expand=False,\n        )\n        aggregated.extend(\n            self._to_calendar_event(r, calendar_id=cal.name) for r in results\n        )\n    return aggregated\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient.list_tasks","title":"<code>list_tasks(calendar_id: str = '') -&gt; list[Task]</code>","text":"<p>Return all VTODO tasks from CalDAV calendar collections.</p> <p>When calendar_id is empty, tasks are aggregated from all calendars.  Each task is tagged with its source <code>calendar_id</code>.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list tasks\")\ndef list_tasks(self, calendar_id: str = \"\") -&gt; list[Task]:\n    \"\"\"Return all VTODO tasks from CalDAV calendar collections.\n\n    When *calendar_id* is empty, tasks are aggregated from **all**\n    calendars.  Each task is tagged with its source ``calendar_id``.\n    \"\"\"\n    logger.debug(\"list_tasks calendar_id=%r\", calendar_id)\n    aggregated: list[Task] = []\n    for cal in self._iter_calendars(calendar_id):\n        results = cal.search(todo=True)\n        aggregated.extend(self._to_task(r, calendar_id=cal.name) for r in results)\n    return aggregated\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient.update_contact","title":"<code>update_contact(uid: str, contact: Contact, addressbook_id: str = '') -&gt; Contact</code>","text":"<p>Update the contact identified by uid; return the updated contact.</p> <p>Raises:</p> Type Description <code>OperationError</code> <p>If the UID doesn't exist (code <code>\"not_found\"</code>).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"update contact\")\ndef update_contact(\n    self, uid: str, contact: Contact, addressbook_id: str = \"\"\n) -&gt; Contact:\n    \"\"\"Update the contact identified by *uid*; return the updated contact.\n\n    Raises:\n        OperationError: If the UID doesn't exist (code ``\"not_found\"``).\n    \"\"\"\n    logger.debug(\n        \"update_contact uid=%r addressbook_id=%r full_name=%r\",\n        uid,\n        addressbook_id,\n        contact.full_name,\n    )\n    ab = self._get_addressbook(addressbook_id)\n    # Fetch to confirm existence \u2014 caldav addressbook search by UID\n    existing = ab.search(f\"UID:{uid}\")\n    if not existing:\n        raise OperationError(\n            code=\"not_found\",\n            message=f\"Contact with UID {uid!r} not found.\",\n        )\n    # Delete the old vcard and create a new one\n    existing[0].delete()\n    updated = Contact(\n        uid=uid,\n        full_name=contact.full_name,\n        email=contact.email,\n        phone=contact.phone,\n        address=contact.address,\n        addressbook_id=addressbook_id,\n    )\n    vcard = self._contact_to_vcard(updated)\n    saved = ab.save_object(vcard)\n    return self._to_contact(saved, addressbook_id=ab.name)\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalDavClient.update_event","title":"<code>update_event(uid: str, event: CalendarEvent, calendar_id: str = '') -&gt; CalendarEvent</code>","text":"<p>Update the event identified by uid; return the updated event.</p> <p>When calendar_id is empty, iterates all calendars to locate the UID (the UID may live in a non-default collection).  When calendar_id is given, only that single calendar is searched.</p> <p>Raises:</p> Type Description <code>OperationError</code> <p>If the UID doesn't exist (code <code>\"not_found\"</code>).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"update event\")\ndef update_event(\n    self, uid: str, event: CalendarEvent, calendar_id: str = \"\"\n) -&gt; CalendarEvent:\n    \"\"\"Update the event identified by *uid*; return the updated event.\n\n    When *calendar_id* is empty, iterates **all** calendars to locate\n    the UID (the UID may live in a non-default collection).  When\n    *calendar_id* is given, only that single calendar is searched.\n\n    Raises:\n        OperationError: If the UID doesn't exist (code ``\"not_found\"``).\n    \"\"\"\n    logger.debug(\n        \"update_event uid=%r calendar_id=%r summary=%r\",\n        uid,\n        calendar_id,\n        event.summary,\n    )\n    if calendar_id:\n        cal = self._get_calendar(calendar_id)\n        existing = cal.event(uid=uid)\n        if existing is None:\n            raise OperationError(\n                code=\"not_found\",\n                message=f\"Event with UID {uid!r} not found.\",\n            )\n    else:\n        # Locate the UID across all calendars.\n        cal = None\n        for candidate in self._iter_calendars(\"\"):\n            if candidate.event(uid=uid) is not None:\n                cal = candidate\n                break\n        if cal is None:\n            raise OperationError(\n                code=\"not_found\",\n                message=f\"Event with UID {uid!r} not found.\",\n            )\n    # Build updated iCal with the same UID\n    updated = CalendarEvent(\n        uid=uid,\n        summary=event.summary,\n        description=event.description,\n        location=event.location,\n        dtstart=event.dtstart,\n        dtend=event.dtend,\n        calendar_id=calendar_id or cal.name,\n    )\n    ical = self._event_to_ical(updated)\n    saved = cal.save_event(ical)\n    return self._to_calendar_event(saved, calendar_id=cal.name)\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalendarAgent","title":"<code>CalendarAgent(agent_id: str = 'calendar', *, radicale_url: str | None = None, radicale_username: str | None = None, radicale_password: str | None = None, llm_model_config: dict[str, Any] | None = None, agent: Any | None = None, component_responder: Any | None = None)</code>","text":"<p>Top-level agent that processes calendar/contact instructions.</p> <p>Creates a :class:<code>CalDavClient</code>, :class:<code>IntentParser</code>, and an agent-comm :class:<code>Agent</code>, then registers an <code>on_request</code> handler that parses intent, dispatches to CalDAV/CardDAV, and returns a correlated response.</p> <p>Parameters:</p> Name Type Description Default <code>agent_id</code> <code>str</code> <p>Agent-comm agent ID (default <code>\"calendar\"</code>).</p> <code>'calendar'</code> <code>radicale_url</code> <code>str | None</code> <p>Radicale server URL (falls back to env <code>RADICALE_URL</code>).</p> <code>None</code> <code>radicale_username</code> <code>str | None</code> <p>Radicale username (falls back to env <code>RADICALE_USERNAME</code>).</p> <code>None</code> <code>radicale_password</code> <code>str | None</code> <p>Radicale password (falls back to env <code>RADICALE_PASSWORD</code>).</p> <code>None</code> <code>llm_model_config</code> <code>dict[str, Any] | None</code> <p>Forwarded to :class:<code>IntentParser</code> for llmio model selection.</p> <code>None</code> <code>agent</code> <code>Any | None</code> <p>The agent-comm client to wire the request handler onto and drive via :meth:<code>start</code>/:meth:<code>stop</code> \u2014 a :class:<code>robotsix_agent_comm.sdk.BrokeredAgent</code> for the brokered service (see <code>brokered_entrypoint</code>), or any object exposing <code>on_request</code>/<code>start</code>/<code>stop</code>. When <code>None</code> (the default), an in-process :class:<code>robotsix_agent_comm.sdk.Agent</code> is created for back-compat (tests / single-process use).</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If Radicale credentials are missing after constructor-arg + env-var fallback.</p> Source code in <code>src/robotsix_calendar_agent/agent.py</code> <pre><code>def __init__(\n    self,\n    agent_id: str = \"calendar\",\n    *,\n    radicale_url: str | None = None,\n    radicale_username: str | None = None,\n    radicale_password: str | None = None,\n    llm_model_config: dict[str, Any] | None = None,\n    agent: Any | None = None,\n    component_responder: Any | None = None,\n) -&gt; None:\n    from .settings import Settings\n\n    settings = Settings()\n\n    self._agent_id = agent_id\n    self._settings = settings\n\n    url = radicale_url or settings.RADICALE_URL\n    username = radicale_username or settings.RADICALE_USERNAME\n    password = radicale_password or settings.RADICALE_PASSWORD.get_secret_value()\n\n    if not url or not username or not password:\n        _MISSING_CREDENTIALS_MSG = (\n            \"Radicale credentials are required. \"\n            \"Set RADICALE_URL, RADICALE_USERNAME, RADICALE_PASSWORD \"\n            \"environment variables or pass them as constructor arguments.\"\n        )\n        raise ValueError(_MISSING_CREDENTIALS_MSG)\n\n    default_calendar = settings.RADICALE_DEFAULT_CALENDAR\n    self._caldav = CalDavClient(\n        url, username, password, default_calendar=default_calendar\n    )\n    self._intent_parser = IntentParser(model_config=llm_model_config)\n\n    if agent is None:\n        from robotsix_agent_comm.sdk import Agent as AgentCommAgent\n        from robotsix_agent_comm.transport import Registry\n\n        agent = AgentCommAgent(agent_id, Registry())\n\n    self._agent = agent\n    self._agent.on_request(self._handle_request)\n\n    # -- telemetry -------------------------------------------------\n    self._started_at: float = time.monotonic()\n    self._request_count: int = 0\n    self._error_count: int = 0\n    self._last_request_ts: float | None = None\n    self._in_flight: int = 0\n\n    # -- optional component-agent responder ------------------------\n    self._component_responder = component_responder\n    if self._component_responder is not None:\n        self._component_responder.set_agent(self)\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalendarAgent.monitor_snapshot","title":"<code>monitor_snapshot() -&gt; dict[str, Any]</code>","text":"<p>Return a real-time snapshot of agent telemetry.</p> <p>Includes live counters, timestamps, calendar/runtime facts, and a CalDAV health probe.  Called by the component-agent responder on every <code>monitor</code> request \u2014 never cached.</p> Source code in <code>src/robotsix_calendar_agent/agent.py</code> <pre><code>def monitor_snapshot(self) -&gt; dict[str, Any]:\n    \"\"\"Return a real-time snapshot of agent telemetry.\n\n    Includes live counters, timestamps, calendar/runtime facts, and a\n    CalDAV health probe.  Called by the component-agent responder on\n    every ``monitor`` request \u2014 never cached.\n    \"\"\"\n    uptime = time.monotonic() - self._started_at\n    health = self._caldav.health()\n    return {\n        \"agent_id\": self._agent_id,\n        \"uptime_seconds\": round(uptime, 3),\n        \"request_count\": self._request_count,\n        \"error_count\": self._error_count,\n        \"in_flight\": self._in_flight,\n        \"last_request_ts\": self._last_request_ts,\n        \"caldav_url\": self._caldav._url,\n        \"default_calendar\": self._caldav._default_calendar,\n        \"caldav_health\": health,\n    }\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalendarAgent.start","title":"<code>start() -&gt; None</code>","text":"<p>Start the agent-comm transport and register the endpoint.</p> Source code in <code>src/robotsix_calendar_agent/agent.py</code> <pre><code>def start(self) -&gt; None:\n    \"\"\"Start the agent-comm transport and register the endpoint.\"\"\"\n    logger.info(\"Starting CalendarAgent (agent_id=%r)\", self._agent_id)\n    self._agent.start()\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalendarAgent.stop","title":"<code>stop() -&gt; None</code>","text":"<p>Stop the agent-comm transport and unregister.</p> Source code in <code>src/robotsix_calendar_agent/agent.py</code> <pre><code>def stop(self) -&gt; None:\n    \"\"\"Stop the agent-comm transport and unregister.\"\"\"\n    logger.info(\"Stopping CalendarAgent (agent_id=%r)\", self._agent_id)\n    self._agent.stop()\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.CalendarEvent","title":"<code>CalendarEvent(*, uid: str = '', summary: str, description: str = '', location: str = '', dtstart: str, dtend: str, calendar_id: str = '')</code>  <code>dataclass</code>","text":"<p>Calendar event data transferred to/from the CalDAV server.</p> <p>Attributes:</p> Name Type Description <code>uid</code> <code>str</code> <p>Server-assigned unique identifier (empty for new events).</p> <code>summary</code> <code>str</code> <p>Short summary / title.</p> <code>description</code> <code>str</code> <p>Longer description (optional).</p> <code>location</code> <code>str</code> <p>Event location (optional).</p> <code>dtstart</code> <code>str</code> <p>Start time in ISO 8601 format.</p> <code>dtend</code> <code>str</code> <p>End time in ISO 8601 format.</p> <code>calendar_id</code> <code>str</code> <p>Calendar name the event belongs to (optional).</p>"},{"location":"reference/#robotsix_calendar_agent.CalendarOperation","title":"<code>CalendarOperation</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Calendar (CalDAV) operation types.</p>"},{"location":"reference/#robotsix_calendar_agent.Contact","title":"<code>Contact(*, uid: str = '', full_name: str, email: str = '', phone: str = '', address: str = '', addressbook_id: str = '')</code>  <code>dataclass</code>","text":"<p>Contact data transferred to/from the CardDAV server.</p> <p>Attributes:</p> Name Type Description <code>uid</code> <code>str</code> <p>Server-assigned unique identifier (empty for new contacts).</p> <code>full_name</code> <code>str</code> <p>Display name.</p> <code>email</code> <code>str</code> <p>Email address (optional).</p> <code>phone</code> <code>str</code> <p>Phone number (optional).</p> <code>address</code> <code>str</code> <p>Postal address (optional).</p> <code>addressbook_id</code> <code>str</code> <p>Address book name the contact belongs to (optional).</p>"},{"location":"reference/#robotsix_calendar_agent.ContactOperation","title":"<code>ContactOperation</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Contacts (CardDAV) operation types.</p>"},{"location":"reference/#robotsix_calendar_agent.IntentParseError","title":"<code>IntentParseError(message: str)</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when intent parsing fails (llmio error or unclassifiable input).</p> Source code in <code>src/robotsix_calendar_agent/intent_parser.py</code> <pre><code>def __init__(self, message: str) -&gt; None:\n    super().__init__(message)\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.IntentParser","title":"<code>IntentParser(model_config: dict[str, Any] | None = None)</code>","text":"<p>Parser that uses <code>robotsix-llmio</code> to classify instructions.</p> <p>Parameters:</p> Name Type Description Default <code>model_config</code> <code>dict[str, Any] | None</code> <p>Forwarded to the llmio provider for model selection/temperature/etc.  If <code>None</code>, llmio defaults apply.</p> <code>None</code> Source code in <code>src/robotsix_calendar_agent/intent_parser.py</code> <pre><code>def __init__(self, model_config: dict[str, Any] | None = None) -&gt; None:\n    self._model_config = model_config or {}\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.IntentParser.parse","title":"<code>parse(text: str) -&gt; ParsedIntent</code>","text":"<p>Parse natural-language text into a :class:<code>ParsedIntent</code>.</p> <p>Raises:</p> Type Description <code>IntentParseError</code> <p>If the llmio call fails or the model cannot produce a valid structured result.</p> Source code in <code>src/robotsix_calendar_agent/intent_parser.py</code> <pre><code>def parse(self, text: str) -&gt; ParsedIntent:\n    \"\"\"Parse natural-language *text* into a :class:`ParsedIntent`.\n\n    Raises:\n        IntentParseError: If the llmio call fails or the model cannot\n            produce a valid structured result.\n    \"\"\"\n    try:\n        from robotsix_llmio.core import build_agent_for_level, run_agent\n\n        # level=2 is the standard LLMIO default (DeepSeek V4 Pro).\n        # The implement agent already uses this tier successfully for\n        # tool calls and structured output via pydantic-ai.\n        handle = build_agent_for_level(\n            2,\n            provider_kwargs=self._model_config or None,\n            system_prompt=_INTENT_SYSTEM_PROMPT,\n            output_type=_IntentOutput,\n        )\n\n        def _run() -&gt; _IntentOutput:\n            # AgentHandle delegates attribute access to the wrapped\n            # pydantic-ai Agent, so call run_sync on the handle directly.\n            # pydantic-ai &gt;=1.0 exposes the result on ``.output``.\n            result = handle.run_sync(text)\n            return result.output  # type: ignore[no-any-return]\n\n        output = run_agent(\n            handle,\n            _run,\n            label=\"intent-parse\",\n            what=\"intent parsing\",\n        )\n        if not isinstance(output, _IntentOutput):\n            _msg = f\"Unexpected output type from llmio: {type(output)}\"\n            raise IntentParseError(_msg)\n\n        result = ParsedIntent(\n            operation=output.operation,  # type: ignore[arg-type]\n            params=output.params or {},\n            original_text=text,\n        )\n        logger.info(\"Parsed intent: operation=%r text=%r\", result.operation, text)\n        return result\n    except IntentParseError as exc:\n        logger.exception(\"Intent parse error for '%s': %s\", text, exc)\n        raise\n    except Exception as exc:\n        logger.exception(\n            \"Unexpected error during intent parsing for '%s': %s\", text, exc\n        )\n        _msg = f\"Intent parsing failed: {exc}\"\n        raise IntentParseError(_msg) from exc\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.OperationError","title":"<code>OperationError(code: str, message: str)</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a CalDAV/CardDAV operation fails.</p> <p>Attributes:</p> Name Type Description <code>code</code> <p>Machine-readable error code (e.g. <code>\"not_found\"</code>, <code>\"auth_failed\"</code>, <code>\"caldav_error\"</code>).</p> <code>message</code> <p>Human-readable error description.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>def __init__(self, code: str, message: str) -&gt; None:\n    super().__init__(message)\n    self.code = code\n    self.message = message\n</code></pre>"},{"location":"reference/#robotsix_calendar_agent.ParsedIntent","title":"<code>ParsedIntent(*, operation: CalendarOperation | ContactOperation | TaskOperation, params: dict[str, Any] = dict(), original_text: str = '')</code>  <code>dataclass</code>","text":"<p>Result of parsing a user instruction.</p> <p>Attributes:</p> Name Type Description <code>operation</code> <code>CalendarOperation | ContactOperation | TaskOperation</code> <p>The classified operation.</p> <code>params</code> <code>dict[str, Any]</code> <p>Operation-specific arguments (dates, uids, fields, ...).</p> <code>original_text</code> <code>str</code> <p>The raw instruction string, preserved for logging.</p>"},{"location":"reference/#robotsix_calendar_agent.Task","title":"<code>Task(*, uid: str = '', summary: str, description: str = '', dtstart: str = '', due: str = '', status: str = '', calendar_id: str = '')</code>  <code>dataclass</code>","text":"<p>Represents a single VTODO task from a CalDAV calendar.</p> <p>Attributes:</p> Name Type Description <code>uid</code> <code>str</code> <p>Server-assigned unique identifier (empty for new tasks).</p> <code>summary</code> <code>str</code> <p>Short summary / title.</p> <code>description</code> <code>str</code> <p>Longer description (optional).</p> <code>dtstart</code> <code>str</code> <p>When the task starts (optional, ISO 8601).</p> <code>due</code> <code>str</code> <p>Task due date/time (VTODO DUE field, optional, ISO 8601).</p> <code>status</code> <code>str</code> <p>Task status \u2014 e.g. NEEDS-ACTION, IN-PROCESS, COMPLETED, CANCELLED.</p> <code>calendar_id</code> <code>str</code> <p>Calendar name the task belongs to (optional).</p>"},{"location":"reference/#robotsix_calendar_agent.TaskOperation","title":"<code>TaskOperation</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Tasks (CalDAV VTODO) operation types.</p>"},{"location":"reference/add_to_calendar_handler/","title":"Add-to-Calendar Handler","text":""},{"location":"reference/add_to_calendar_handler/#robotsix_calendar_agent.add_to_calendar_handler","title":"<code>robotsix_calendar_agent.add_to_calendar_handler</code>","text":"<p>Structured add-to-calendar handler.</p> <p>Handles <code>add_to_calendar</code> request payloads from auto-mail, validating and creating calendar events via the CalDAV client. Concrete start/end datetimes are taken from explicit <code>suggested_dt*</code> fields when present, otherwise resolved from the forwarded email context via the LLM intent parser.</p>"},{"location":"reference/add_to_calendar_handler/#robotsix_calendar_agent.add_to_calendar_handler.handle_add_to_calendar","title":"<code>handle_add_to_calendar(caldav_client: Any, request: Any, payload: dict[str, Any], *, intent_parser: Any | None = None) -&gt; Any</code>","text":"<p>Handle a structured add-to-calendar request from auto-mail.</p> <p>Validates the payload, creates a :class:<code>CalendarEvent</code> via :meth:<code>CalDavClient.create_event</code>, and returns a correlated :class:<code>Response</code> \u2014 always carrying the <code>correlation_id</code> from the request, whether successful or not.</p> <p>When the payload carries explicit <code>suggested_dtstart</code>/<code>suggested_dtend</code> ISO strings, they are used directly (LLM-free path). Otherwise \u2014 auto-mail's usual case, which forwards only the raw email context \u2014 intent_parser (when provided) resolves concrete start/end datetimes from the subject, body, and extracted date references.</p> Source code in <code>src/robotsix_calendar_agent/add_to_calendar_handler.py</code> <pre><code>def handle_add_to_calendar(\n    caldav_client: Any,\n    request: Any,\n    payload: dict[str, Any],\n    *,\n    intent_parser: Any | None = None,\n) -&gt; Any:\n    \"\"\"Handle a structured add-to-calendar request from auto-mail.\n\n    Validates the payload, creates a :class:`CalendarEvent` via\n    :meth:`CalDavClient.create_event`, and returns a correlated\n    :class:`Response` \u2014 always carrying the ``correlation_id``\n    from the request, whether successful or not.\n\n    When the payload carries explicit ``suggested_dtstart``/``suggested_dtend``\n    ISO strings, they are used directly (LLM-free path). Otherwise \u2014 auto-mail's\n    usual case, which forwards only the raw email context \u2014 *intent_parser*\n    (when provided) resolves concrete start/end datetimes from the subject,\n    body, and extracted date references.\n    \"\"\"\n    import datetime\n\n    from robotsix_agent_comm.protocol import Response\n\n    # -- validation ---------------------------------------------------\n\n    result = _validate_add_to_calendar_payload(payload, request)\n    if not isinstance(result, tuple):\n        return result  # error Response\n    subject, description, location, correlation_id = result\n\n    # -- date resolution ----------------------------------------------\n\n    dates = _resolve_event_dates(payload, intent_parser)\n    if dates is None:\n        return Response.to(\n            request,\n            body=_build_error_body(\n                ERROR_MISSING_DATES,\n                \"Could not determine event start/end times: provide \"\n                \"suggested_dtstart and suggested_dtend, or email content \"\n                \"the calendar agent can resolve a date from.\",\n                correlation_id,\n            ),\n        )\n    dtstart_str, dtend_str = dates\n\n    # -- ISO 8601 parsing + time-ordering -----------------------------\n\n    try:\n        dtstart = datetime.datetime.fromisoformat(dtstart_str)\n        dtend = datetime.datetime.fromisoformat(dtend_str)\n    except (ValueError, TypeError):\n        return Response.to(\n            request,\n            body=_build_error_body(\n                ERROR_INVALID_DATES,\n                \"Cannot parse one or both date strings as ISO 8601.\",\n                correlation_id,\n            ),\n        )\n\n    if dtend &lt;= dtstart:\n        return Response.to(\n            request,\n            body=_build_error_body(\n                ERROR_INVALID_DATES,\n                \"End time must be after start time.\",\n                correlation_id,\n            ),\n        )\n\n    # -- event creation -----------------------------------------------\n\n    created, error_resp = _create_calendar_event(\n        caldav_client,\n        request,\n        subject,\n        description,\n        location,\n        dtstart_str,\n        dtend_str,\n        correlation_id,\n    )\n    if error_resp is not None:\n        return error_resp\n\n    # -- success response ---------------------------------------------\n\n    event_dict = _event_to_dict(created)\n    confirmation_text = (\n        f\"Created event '{created.summary}' on \"\n        f\"{dtstart.strftime('%b %d, %Y at %I:%M %p')}\"\n    )\n\n    logger.info(\"Created event '%s' (uid=%s)\", created.summary, created.uid)\n\n    return Response.to(\n        request,\n        body={\n            \"result\": {\n                \"status\": \"created\",\n                \"event\": event_dict,\n                \"confirmation_text\": confirmation_text,\n            },\n            \"correlation_id\": correlation_id,\n        },\n    )\n</code></pre>"},{"location":"reference/agent/","title":"CalendarAgent","text":""},{"location":"reference/agent/#robotsix_calendar_agent.agent","title":"<code>robotsix_calendar_agent.agent</code>","text":"<p>CalendarAgent \u2014 agent-comm entry point for calendar/contacts management.</p> <p>Wires together :class:<code>IntentParser</code>, :class:<code>CalDavClient</code>, and the agent-comm messaging layer into a single runnable agent.</p>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient","title":"<code>CalDavClient(url: str, username: str, password: str, default_calendar: str = '')</code>","text":"<p>Typed wrapper around <code>caldav.DAVClient</code> for Radicale.</p> <p>All operations convert caldav/requests exceptions to :class:<code>OperationError</code> with appropriate codes.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>Radicale server URL.</p> required <code>username</code> <code>str</code> <p>Radicale username.</p> required <code>password</code> <code>str</code> <p>Radicale password.</p> required <p>Raises:</p> Type Description <code>OperationError</code> <p>If authentication fails.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>def __init__(\n    self, url: str, username: str, password: str, default_calendar: str = \"\"\n) -&gt; None:\n    # ``caldav`` ships incomplete type information (mypy resolves the\n    # module to ``object``), so route it through ``Any`` to keep the\n    # wrapper strict-clean without per-call ignores.\n    import caldav\n\n    _caldav: Any = caldav\n\n    self._url = url\n    self._default_calendar = default_calendar\n    try:\n        self._client = _caldav.DAVClient(\n            url=url, username=username, password=password\n        )\n        self._principal = self._client.principal()\n        logger.info(\"CalDavClient connected to %s as %s\", url, username)\n    except _caldav.error.AuthorizationError as exc:\n        logger.exception(\"CalDAV auth failed for %s as %s: %s\", url, username, exc)\n        raise OperationError(\n            code=\"auth_failed\",\n            message=f\"Authentication failed: {exc}\",\n        ) from exc\n    except Exception as exc:\n        logger.exception(\"Failed to connect to Radicale at %s: %s\", url, exc)\n        raise OperationError(\n            code=\"caldav_error\",\n            message=f\"Failed to connect to Radicale: {exc}\",\n        ) from exc\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient.create_contact","title":"<code>create_contact(contact: Contact, addressbook_id: str = '') -&gt; Contact</code>","text":"<p>Create a contact; return the contact with server-assigned uid.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"create contact\")\ndef create_contact(self, contact: Contact, addressbook_id: str = \"\") -&gt; Contact:\n    \"\"\"Create a contact; return the contact with server-assigned uid.\"\"\"\n    logger.debug(\n        \"create_contact uid=%r addressbook_id=%r full_name=%r\",\n        contact.uid,\n        addressbook_id,\n        contact.full_name,\n    )\n    import uuid\n\n    if not contact.uid:\n        contact = Contact(\n            uid=str(uuid.uuid4()),\n            full_name=contact.full_name,\n            email=contact.email,\n            phone=contact.phone,\n            address=contact.address,\n            addressbook_id=contact.addressbook_id,\n        )\n    ab = self._get_addressbook(addressbook_id)\n    vcard = self._contact_to_vcard(contact)\n    saved = ab.save_object(vcard)\n    return self._to_contact(saved, addressbook_id=ab.name)\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient.create_event","title":"<code>create_event(event: CalendarEvent, calendar_id: str = '') -&gt; CalendarEvent</code>","text":"<p>Create an event; return the event with its server-assigned uid.</p> <p>If calendar_id is empty, use the default calendar.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"create event\")\ndef create_event(\n    self, event: CalendarEvent, calendar_id: str = \"\"\n) -&gt; CalendarEvent:\n    \"\"\"Create an event; return the event with its server-assigned uid.\n\n    If *calendar_id* is empty, use the default calendar.\n    \"\"\"\n    logger.debug(\n        \"create_event uid=%r calendar_id=%r summary=%r\",\n        event.uid,\n        calendar_id,\n        event.summary,\n    )\n    import uuid\n\n    if not event.uid:\n        # Generate a temporary UID; the server may replace it.\n        event = CalendarEvent(\n            uid=str(uuid.uuid4()),\n            summary=event.summary,\n            description=event.description,\n            location=event.location,\n            dtstart=event.dtstart,\n            dtend=event.dtend,\n            calendar_id=event.calendar_id,\n        )\n    cal = self._get_calendar(calendar_id)\n    ical = self._event_to_ical(event)\n    saved = cal.save_event(ical)\n    return self._to_calendar_event(saved, calendar_id=cal.name)\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient.delete_contact","title":"<code>delete_contact(uid: str, addressbook_id: str = '') -&gt; None</code>","text":"<p>Delete the contact identified by uid. Idempotent.</p> <p>Returns <code>None</code> when the UID does not exist (already deleted).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"delete contact\")\ndef delete_contact(self, uid: str, addressbook_id: str = \"\") -&gt; None:\n    \"\"\"Delete the contact identified by *uid*. Idempotent.\n\n    Returns ``None`` when the UID does not exist (already deleted).\n    \"\"\"\n    logger.debug(\"delete_contact uid=%r addressbook_id=%r\", uid, addressbook_id)\n    ab = self._get_addressbook(addressbook_id)\n    existing = ab.search(f\"UID:{uid}\")\n    if not existing:\n        return None\n    existing[0].delete()\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient.delete_event","title":"<code>delete_event(uid: str, calendar_id: str = '') -&gt; None</code>","text":"<p>Delete the event identified by uid. Idempotent.</p> <p>When calendar_id is empty, iterates all calendars to locate the UID.  Returns <code>None</code> when the UID does not exist in any calendar (already deleted).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"delete event\")\ndef delete_event(self, uid: str, calendar_id: str = \"\") -&gt; None:\n    \"\"\"Delete the event identified by *uid*. Idempotent.\n\n    When *calendar_id* is empty, iterates **all** calendars to locate\n    the UID.  Returns ``None`` when the UID does not exist in any\n    calendar (already deleted).\n    \"\"\"\n    logger.debug(\"delete_event uid=%r calendar_id=%r\", uid, calendar_id)\n    if calendar_id:\n        cal = self._get_calendar(calendar_id)\n        event_obj = cal.event(uid=uid)\n        if event_obj is None:\n            return None\n        event_obj.delete()\n    else:\n        for cal in self._iter_calendars(\"\"):\n            event_obj = cal.event(uid=uid)\n            if event_obj is not None:\n                event_obj.delete()\n                return None\n        return None\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient.health","title":"<code>health() -&gt; dict[str, Any]</code>","text":"<p>Perform a one-shot CalDAV reachability check.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p><code>{\"connected\": True, \"calendar_count\": int}</code> on success, or</p> <code>dict[str, Any]</code> <p><code>{\"connected\": False, \"error\": str}</code> when the probe fails.</p> <p>This is called on-demand by <code>monitor</code> \u2014 it does not cache stale state.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>def health(self) -&gt; dict[str, Any]:\n    \"\"\"Perform a one-shot CalDAV reachability check.\n\n    Returns:\n        ``{\"connected\": True, \"calendar_count\": int}`` on success, or\n        ``{\"connected\": False, \"error\": str}`` when the probe fails.\n\n    This is called on-demand by ``monitor`` \u2014 it does **not** cache\n    stale state.\n    \"\"\"\n    try:\n        calendars = self._principal.calendars()\n    except Exception as exc:\n        logger.warning(\"CalDAV health probe failed: %s\", exc)\n        return {\"connected\": False, \"error\": str(exc)}\n    else:\n        return {\"connected\": True, \"calendar_count\": len(calendars)}\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient.list_calendars","title":"<code>list_calendars() -&gt; list[str]</code>","text":"<p>Return the names of all available calendar collections.</p> <p>Only VEVENT / calendar collections are included (addressbooks come from the separate <code>self._principal.addressbooks()</code> and are inherently excluded).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list calendars\")\ndef list_calendars(self) -&gt; list[str]:\n    \"\"\"Return the names of all available calendar collections.\n\n    Only VEVENT / calendar collections are included (addressbooks\n    come from the separate ``self._principal.addressbooks()`` and\n    are inherently excluded).\n    \"\"\"\n    return [cal.name for cal in self._principal.calendars()]\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient.list_contacts","title":"<code>list_contacts(addressbook_id: str = '') -&gt; list[Contact]</code>","text":"<p>Return all contacts.</p> <p>If addressbook_id is empty, use the default address book.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list contacts\")\ndef list_contacts(self, addressbook_id: str = \"\") -&gt; list[Contact]:\n    \"\"\"Return all contacts.\n\n    If *addressbook_id* is empty, use the default address book.\n    \"\"\"\n    logger.debug(\"list_contacts addressbook_id=%r\", addressbook_id)\n    ab = self._get_addressbook(addressbook_id)\n    results = ab.search()\n    return [self._to_contact(r, addressbook_id=ab.name) for r in results]\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient.list_events","title":"<code>list_events(start: str, end: str, calendar_id: str = '') -&gt; list[CalendarEvent]</code>","text":"<p>Return events in the ISO 8601 date range.</p> <p>When calendar_id is empty, events are aggregated from all calendars.  Each event is tagged with its source <code>calendar_id</code>.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list events\")\ndef list_events(\n    self, start: str, end: str, calendar_id: str = \"\"\n) -&gt; list[CalendarEvent]:\n    \"\"\"Return events in the ISO 8601 date range.\n\n    When *calendar_id* is empty, events are aggregated from **all**\n    calendars.  Each event is tagged with its source ``calendar_id``.\n    \"\"\"\n    logger.debug(\n        \"list_events start=%r end=%r calendar_id=%r\",\n        start,\n        end,\n        calendar_id,\n    )\n    start_dt, end_dt = self._iso_date_range(start, end)\n    aggregated: list[CalendarEvent] = []\n    for cal in self._iter_calendars(calendar_id):\n        results = cal.search(\n            start=start_dt,\n            end=end_dt,\n            event=True,\n            expand=False,\n        )\n        aggregated.extend(\n            self._to_calendar_event(r, calendar_id=cal.name) for r in results\n        )\n    return aggregated\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient.list_tasks","title":"<code>list_tasks(calendar_id: str = '') -&gt; list[Task]</code>","text":"<p>Return all VTODO tasks from CalDAV calendar collections.</p> <p>When calendar_id is empty, tasks are aggregated from all calendars.  Each task is tagged with its source <code>calendar_id</code>.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list tasks\")\ndef list_tasks(self, calendar_id: str = \"\") -&gt; list[Task]:\n    \"\"\"Return all VTODO tasks from CalDAV calendar collections.\n\n    When *calendar_id* is empty, tasks are aggregated from **all**\n    calendars.  Each task is tagged with its source ``calendar_id``.\n    \"\"\"\n    logger.debug(\"list_tasks calendar_id=%r\", calendar_id)\n    aggregated: list[Task] = []\n    for cal in self._iter_calendars(calendar_id):\n        results = cal.search(todo=True)\n        aggregated.extend(self._to_task(r, calendar_id=cal.name) for r in results)\n    return aggregated\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient.update_contact","title":"<code>update_contact(uid: str, contact: Contact, addressbook_id: str = '') -&gt; Contact</code>","text":"<p>Update the contact identified by uid; return the updated contact.</p> <p>Raises:</p> Type Description <code>OperationError</code> <p>If the UID doesn't exist (code <code>\"not_found\"</code>).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"update contact\")\ndef update_contact(\n    self, uid: str, contact: Contact, addressbook_id: str = \"\"\n) -&gt; Contact:\n    \"\"\"Update the contact identified by *uid*; return the updated contact.\n\n    Raises:\n        OperationError: If the UID doesn't exist (code ``\"not_found\"``).\n    \"\"\"\n    logger.debug(\n        \"update_contact uid=%r addressbook_id=%r full_name=%r\",\n        uid,\n        addressbook_id,\n        contact.full_name,\n    )\n    ab = self._get_addressbook(addressbook_id)\n    # Fetch to confirm existence \u2014 caldav addressbook search by UID\n    existing = ab.search(f\"UID:{uid}\")\n    if not existing:\n        raise OperationError(\n            code=\"not_found\",\n            message=f\"Contact with UID {uid!r} not found.\",\n        )\n    # Delete the old vcard and create a new one\n    existing[0].delete()\n    updated = Contact(\n        uid=uid,\n        full_name=contact.full_name,\n        email=contact.email,\n        phone=contact.phone,\n        address=contact.address,\n        addressbook_id=addressbook_id,\n    )\n    vcard = self._contact_to_vcard(updated)\n    saved = ab.save_object(vcard)\n    return self._to_contact(saved, addressbook_id=ab.name)\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalDavClient.update_event","title":"<code>update_event(uid: str, event: CalendarEvent, calendar_id: str = '') -&gt; CalendarEvent</code>","text":"<p>Update the event identified by uid; return the updated event.</p> <p>When calendar_id is empty, iterates all calendars to locate the UID (the UID may live in a non-default collection).  When calendar_id is given, only that single calendar is searched.</p> <p>Raises:</p> Type Description <code>OperationError</code> <p>If the UID doesn't exist (code <code>\"not_found\"</code>).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"update event\")\ndef update_event(\n    self, uid: str, event: CalendarEvent, calendar_id: str = \"\"\n) -&gt; CalendarEvent:\n    \"\"\"Update the event identified by *uid*; return the updated event.\n\n    When *calendar_id* is empty, iterates **all** calendars to locate\n    the UID (the UID may live in a non-default collection).  When\n    *calendar_id* is given, only that single calendar is searched.\n\n    Raises:\n        OperationError: If the UID doesn't exist (code ``\"not_found\"``).\n    \"\"\"\n    logger.debug(\n        \"update_event uid=%r calendar_id=%r summary=%r\",\n        uid,\n        calendar_id,\n        event.summary,\n    )\n    if calendar_id:\n        cal = self._get_calendar(calendar_id)\n        existing = cal.event(uid=uid)\n        if existing is None:\n            raise OperationError(\n                code=\"not_found\",\n                message=f\"Event with UID {uid!r} not found.\",\n            )\n    else:\n        # Locate the UID across all calendars.\n        cal = None\n        for candidate in self._iter_calendars(\"\"):\n            if candidate.event(uid=uid) is not None:\n                cal = candidate\n                break\n        if cal is None:\n            raise OperationError(\n                code=\"not_found\",\n                message=f\"Event with UID {uid!r} not found.\",\n            )\n    # Build updated iCal with the same UID\n    updated = CalendarEvent(\n        uid=uid,\n        summary=event.summary,\n        description=event.description,\n        location=event.location,\n        dtstart=event.dtstart,\n        dtend=event.dtend,\n        calendar_id=calendar_id or cal.name,\n    )\n    ical = self._event_to_ical(updated)\n    saved = cal.save_event(ical)\n    return self._to_calendar_event(saved, calendar_id=cal.name)\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalendarAgent","title":"<code>CalendarAgent(agent_id: str = 'calendar', *, radicale_url: str | None = None, radicale_username: str | None = None, radicale_password: str | None = None, llm_model_config: dict[str, Any] | None = None, agent: Any | None = None, component_responder: Any | None = None)</code>","text":"<p>Top-level agent that processes calendar/contact instructions.</p> <p>Creates a :class:<code>CalDavClient</code>, :class:<code>IntentParser</code>, and an agent-comm :class:<code>Agent</code>, then registers an <code>on_request</code> handler that parses intent, dispatches to CalDAV/CardDAV, and returns a correlated response.</p> <p>Parameters:</p> Name Type Description Default <code>agent_id</code> <code>str</code> <p>Agent-comm agent ID (default <code>\"calendar\"</code>).</p> <code>'calendar'</code> <code>radicale_url</code> <code>str | None</code> <p>Radicale server URL (falls back to env <code>RADICALE_URL</code>).</p> <code>None</code> <code>radicale_username</code> <code>str | None</code> <p>Radicale username (falls back to env <code>RADICALE_USERNAME</code>).</p> <code>None</code> <code>radicale_password</code> <code>str | None</code> <p>Radicale password (falls back to env <code>RADICALE_PASSWORD</code>).</p> <code>None</code> <code>llm_model_config</code> <code>dict[str, Any] | None</code> <p>Forwarded to :class:<code>IntentParser</code> for llmio model selection.</p> <code>None</code> <code>agent</code> <code>Any | None</code> <p>The agent-comm client to wire the request handler onto and drive via :meth:<code>start</code>/:meth:<code>stop</code> \u2014 a :class:<code>robotsix_agent_comm.sdk.BrokeredAgent</code> for the brokered service (see <code>brokered_entrypoint</code>), or any object exposing <code>on_request</code>/<code>start</code>/<code>stop</code>. When <code>None</code> (the default), an in-process :class:<code>robotsix_agent_comm.sdk.Agent</code> is created for back-compat (tests / single-process use).</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If Radicale credentials are missing after constructor-arg + env-var fallback.</p> Source code in <code>src/robotsix_calendar_agent/agent.py</code> <pre><code>def __init__(\n    self,\n    agent_id: str = \"calendar\",\n    *,\n    radicale_url: str | None = None,\n    radicale_username: str | None = None,\n    radicale_password: str | None = None,\n    llm_model_config: dict[str, Any] | None = None,\n    agent: Any | None = None,\n    component_responder: Any | None = None,\n) -&gt; None:\n    from .settings import Settings\n\n    settings = Settings()\n\n    self._agent_id = agent_id\n    self._settings = settings\n\n    url = radicale_url or settings.RADICALE_URL\n    username = radicale_username or settings.RADICALE_USERNAME\n    password = radicale_password or settings.RADICALE_PASSWORD.get_secret_value()\n\n    if not url or not username or not password:\n        _MISSING_CREDENTIALS_MSG = (\n            \"Radicale credentials are required. \"\n            \"Set RADICALE_URL, RADICALE_USERNAME, RADICALE_PASSWORD \"\n            \"environment variables or pass them as constructor arguments.\"\n        )\n        raise ValueError(_MISSING_CREDENTIALS_MSG)\n\n    default_calendar = settings.RADICALE_DEFAULT_CALENDAR\n    self._caldav = CalDavClient(\n        url, username, password, default_calendar=default_calendar\n    )\n    self._intent_parser = IntentParser(model_config=llm_model_config)\n\n    if agent is None:\n        from robotsix_agent_comm.sdk import Agent as AgentCommAgent\n        from robotsix_agent_comm.transport import Registry\n\n        agent = AgentCommAgent(agent_id, Registry())\n\n    self._agent = agent\n    self._agent.on_request(self._handle_request)\n\n    # -- telemetry -------------------------------------------------\n    self._started_at: float = time.monotonic()\n    self._request_count: int = 0\n    self._error_count: int = 0\n    self._last_request_ts: float | None = None\n    self._in_flight: int = 0\n\n    # -- optional component-agent responder ------------------------\n    self._component_responder = component_responder\n    if self._component_responder is not None:\n        self._component_responder.set_agent(self)\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalendarAgent.monitor_snapshot","title":"<code>monitor_snapshot() -&gt; dict[str, Any]</code>","text":"<p>Return a real-time snapshot of agent telemetry.</p> <p>Includes live counters, timestamps, calendar/runtime facts, and a CalDAV health probe.  Called by the component-agent responder on every <code>monitor</code> request \u2014 never cached.</p> Source code in <code>src/robotsix_calendar_agent/agent.py</code> <pre><code>def monitor_snapshot(self) -&gt; dict[str, Any]:\n    \"\"\"Return a real-time snapshot of agent telemetry.\n\n    Includes live counters, timestamps, calendar/runtime facts, and a\n    CalDAV health probe.  Called by the component-agent responder on\n    every ``monitor`` request \u2014 never cached.\n    \"\"\"\n    uptime = time.monotonic() - self._started_at\n    health = self._caldav.health()\n    return {\n        \"agent_id\": self._agent_id,\n        \"uptime_seconds\": round(uptime, 3),\n        \"request_count\": self._request_count,\n        \"error_count\": self._error_count,\n        \"in_flight\": self._in_flight,\n        \"last_request_ts\": self._last_request_ts,\n        \"caldav_url\": self._caldav._url,\n        \"default_calendar\": self._caldav._default_calendar,\n        \"caldav_health\": health,\n    }\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalendarAgent.start","title":"<code>start() -&gt; None</code>","text":"<p>Start the agent-comm transport and register the endpoint.</p> Source code in <code>src/robotsix_calendar_agent/agent.py</code> <pre><code>def start(self) -&gt; None:\n    \"\"\"Start the agent-comm transport and register the endpoint.\"\"\"\n    logger.info(\"Starting CalendarAgent (agent_id=%r)\", self._agent_id)\n    self._agent.start()\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalendarAgent.stop","title":"<code>stop() -&gt; None</code>","text":"<p>Stop the agent-comm transport and unregister.</p> Source code in <code>src/robotsix_calendar_agent/agent.py</code> <pre><code>def stop(self) -&gt; None:\n    \"\"\"Stop the agent-comm transport and unregister.\"\"\"\n    logger.info(\"Stopping CalendarAgent (agent_id=%r)\", self._agent_id)\n    self._agent.stop()\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalendarEvent","title":"<code>CalendarEvent(*, uid: str = '', summary: str, description: str = '', location: str = '', dtstart: str, dtend: str, calendar_id: str = '')</code>  <code>dataclass</code>","text":"<p>Calendar event data transferred to/from the CalDAV server.</p> <p>Attributes:</p> Name Type Description <code>uid</code> <code>str</code> <p>Server-assigned unique identifier (empty for new events).</p> <code>summary</code> <code>str</code> <p>Short summary / title.</p> <code>description</code> <code>str</code> <p>Longer description (optional).</p> <code>location</code> <code>str</code> <p>Event location (optional).</p> <code>dtstart</code> <code>str</code> <p>Start time in ISO 8601 format.</p> <code>dtend</code> <code>str</code> <p>End time in ISO 8601 format.</p> <code>calendar_id</code> <code>str</code> <p>Calendar name the event belongs to (optional).</p>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.CalendarOperation","title":"<code>CalendarOperation</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Calendar (CalDAV) operation types.</p>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.Contact","title":"<code>Contact(*, uid: str = '', full_name: str, email: str = '', phone: str = '', address: str = '', addressbook_id: str = '')</code>  <code>dataclass</code>","text":"<p>Contact data transferred to/from the CardDAV server.</p> <p>Attributes:</p> Name Type Description <code>uid</code> <code>str</code> <p>Server-assigned unique identifier (empty for new contacts).</p> <code>full_name</code> <code>str</code> <p>Display name.</p> <code>email</code> <code>str</code> <p>Email address (optional).</p> <code>phone</code> <code>str</code> <p>Phone number (optional).</p> <code>address</code> <code>str</code> <p>Postal address (optional).</p> <code>addressbook_id</code> <code>str</code> <p>Address book name the contact belongs to (optional).</p>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.ContactOperation","title":"<code>ContactOperation</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Contacts (CardDAV) operation types.</p>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.IntentParseError","title":"<code>IntentParseError(message: str)</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when intent parsing fails (llmio error or unclassifiable input).</p> Source code in <code>src/robotsix_calendar_agent/intent_parser.py</code> <pre><code>def __init__(self, message: str) -&gt; None:\n    super().__init__(message)\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.IntentParser","title":"<code>IntentParser(model_config: dict[str, Any] | None = None)</code>","text":"<p>Parser that uses <code>robotsix-llmio</code> to classify instructions.</p> <p>Parameters:</p> Name Type Description Default <code>model_config</code> <code>dict[str, Any] | None</code> <p>Forwarded to the llmio provider for model selection/temperature/etc.  If <code>None</code>, llmio defaults apply.</p> <code>None</code> Source code in <code>src/robotsix_calendar_agent/intent_parser.py</code> <pre><code>def __init__(self, model_config: dict[str, Any] | None = None) -&gt; None:\n    self._model_config = model_config or {}\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.IntentParser.parse","title":"<code>parse(text: str) -&gt; ParsedIntent</code>","text":"<p>Parse natural-language text into a :class:<code>ParsedIntent</code>.</p> <p>Raises:</p> Type Description <code>IntentParseError</code> <p>If the llmio call fails or the model cannot produce a valid structured result.</p> Source code in <code>src/robotsix_calendar_agent/intent_parser.py</code> <pre><code>def parse(self, text: str) -&gt; ParsedIntent:\n    \"\"\"Parse natural-language *text* into a :class:`ParsedIntent`.\n\n    Raises:\n        IntentParseError: If the llmio call fails or the model cannot\n            produce a valid structured result.\n    \"\"\"\n    try:\n        from robotsix_llmio.core import build_agent_for_level, run_agent\n\n        # level=2 is the standard LLMIO default (DeepSeek V4 Pro).\n        # The implement agent already uses this tier successfully for\n        # tool calls and structured output via pydantic-ai.\n        handle = build_agent_for_level(\n            2,\n            provider_kwargs=self._model_config or None,\n            system_prompt=_INTENT_SYSTEM_PROMPT,\n            output_type=_IntentOutput,\n        )\n\n        def _run() -&gt; _IntentOutput:\n            # AgentHandle delegates attribute access to the wrapped\n            # pydantic-ai Agent, so call run_sync on the handle directly.\n            # pydantic-ai &gt;=1.0 exposes the result on ``.output``.\n            result = handle.run_sync(text)\n            return result.output  # type: ignore[no-any-return]\n\n        output = run_agent(\n            handle,\n            _run,\n            label=\"intent-parse\",\n            what=\"intent parsing\",\n        )\n        if not isinstance(output, _IntentOutput):\n            _msg = f\"Unexpected output type from llmio: {type(output)}\"\n            raise IntentParseError(_msg)\n\n        result = ParsedIntent(\n            operation=output.operation,  # type: ignore[arg-type]\n            params=output.params or {},\n            original_text=text,\n        )\n        logger.info(\"Parsed intent: operation=%r text=%r\", result.operation, text)\n        return result\n    except IntentParseError as exc:\n        logger.exception(\"Intent parse error for '%s': %s\", text, exc)\n        raise\n    except Exception as exc:\n        logger.exception(\n            \"Unexpected error during intent parsing for '%s': %s\", text, exc\n        )\n        _msg = f\"Intent parsing failed: {exc}\"\n        raise IntentParseError(_msg) from exc\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.OperationError","title":"<code>OperationError(code: str, message: str)</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a CalDAV/CardDAV operation fails.</p> <p>Attributes:</p> Name Type Description <code>code</code> <p>Machine-readable error code (e.g. <code>\"not_found\"</code>, <code>\"auth_failed\"</code>, <code>\"caldav_error\"</code>).</p> <code>message</code> <p>Human-readable error description.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>def __init__(self, code: str, message: str) -&gt; None:\n    super().__init__(message)\n    self.code = code\n    self.message = message\n</code></pre>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.ParsedIntent","title":"<code>ParsedIntent(*, operation: CalendarOperation | ContactOperation | TaskOperation, params: dict[str, Any] = dict(), original_text: str = '')</code>  <code>dataclass</code>","text":"<p>Result of parsing a user instruction.</p> <p>Attributes:</p> Name Type Description <code>operation</code> <code>CalendarOperation | ContactOperation | TaskOperation</code> <p>The classified operation.</p> <code>params</code> <code>dict[str, Any]</code> <p>Operation-specific arguments (dates, uids, fields, ...).</p> <code>original_text</code> <code>str</code> <p>The raw instruction string, preserved for logging.</p>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.Task","title":"<code>Task(*, uid: str = '', summary: str, description: str = '', dtstart: str = '', due: str = '', status: str = '', calendar_id: str = '')</code>  <code>dataclass</code>","text":"<p>Represents a single VTODO task from a CalDAV calendar.</p> <p>Attributes:</p> Name Type Description <code>uid</code> <code>str</code> <p>Server-assigned unique identifier (empty for new tasks).</p> <code>summary</code> <code>str</code> <p>Short summary / title.</p> <code>description</code> <code>str</code> <p>Longer description (optional).</p> <code>dtstart</code> <code>str</code> <p>When the task starts (optional, ISO 8601).</p> <code>due</code> <code>str</code> <p>Task due date/time (VTODO DUE field, optional, ISO 8601).</p> <code>status</code> <code>str</code> <p>Task status \u2014 e.g. NEEDS-ACTION, IN-PROCESS, COMPLETED, CANCELLED.</p> <code>calendar_id</code> <code>str</code> <p>Calendar name the task belongs to (optional).</p>"},{"location":"reference/agent/#robotsix_calendar_agent.agent.TaskOperation","title":"<code>TaskOperation</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Tasks (CalDAV VTODO) operation types.</p>"},{"location":"reference/brokered_entrypoint/","title":"Brokered Entrypoint","text":""},{"location":"reference/brokered_entrypoint/#robotsix_calendar_agent.brokered_entrypoint","title":"<code>robotsix_calendar_agent.brokered_entrypoint</code>","text":"<p>Long-lived service entrypoint for :class:<code>CalendarAgent</code>.</p> <p>Selects the transport based on the <code>CALENDAR_AGENT_TRANSPORT</code> environment variable, wires the agent, and blocks until <code>SIGTERM</code>/<code>SIGINT</code> requests a graceful shutdown.</p> <p>Transport modes:</p> <ul> <li><code>inprocess</code> (or unset) \u2014 :class:<code>CalendarAgent</code> builds its own in-process   agent (single-process / tests).</li> <li><code>brokered</code> \u2014 connect to a secured broker over HTTPS using the <code>BROKER_*</code>   environment variables, via the shared   :class:<code>robotsix_agent_comm.sdk.BrokeredAgent</code> (mailbox/pull mode, NAT-safe,   self-healing across broker restarts), registered as <code>robotsix-calendar</code>.</li> </ul> <p>When the component-agent responder is enabled (<code>COMPONENT_AGENT_ENABLED=true</code>), the brokered branch composes a :class:<code>ComponentAgentResponder</code> onto the same <code>BrokeredAgent</code> connection, so <code>monitor</code> / <code>config-get</code> / <code>config-set</code> kinds are served under the existing <code>robotsix-calendar</code> agent identity \u2014 no second broker connection.</p>"},{"location":"reference/brokered_entrypoint/#robotsix_calendar_agent.brokered_entrypoint.ComponentAgentResponder","title":"<code>ComponentAgentResponder(agent: CalendarAgent | None, settings: Any)</code>","text":"<p>Dispatch management-kind requests to live telemetry / config.</p> <p>Holds references to the running agent (for telemetry) and the live settings object (for config read/write).  It is transport-agnostic: it receives a <code>request</code> object and returns a protocol message; the caller owns the broker connection.</p> <p>Parameters:</p> Name Type Description Default <code>agent</code> <code>CalendarAgent | None</code> <p>The running :class:<code>CalendarAgent</code> instance.</p> required <code>settings</code> <code>Any</code> <p>The live :class:<code>~robotsix_calendar_agent.settings.Settings</code> instance the agent was built with.</p> required Source code in <code>src/robotsix_calendar_agent/brokered_entrypoint.py</code> <pre><code>def __init__(self, agent: CalendarAgent | None, settings: Any) -&gt; None:\n    self._agent = agent\n    self._settings = settings\n</code></pre>"},{"location":"reference/brokered_entrypoint/#robotsix_calendar_agent.brokered_entrypoint.ComponentAgentResponder.on_request","title":"<code>on_request(request: Any) -&gt; Any</code>","text":"<p>Handle an incoming management-kind request.</p> <p>Dispatches on <code>request.body.get(\"kind\")</code>.  Unknown or missing kinds receive a broker <code>Error</code>.</p> Source code in <code>src/robotsix_calendar_agent/brokered_entrypoint.py</code> <pre><code>def on_request(self, request: Any) -&gt; Any:\n    \"\"\"Handle an incoming management-kind request.\n\n    Dispatches on ``request.body.get(\"kind\")``.  Unknown or missing\n    kinds receive a broker ``Error``.\n    \"\"\"\n    from robotsix_agent_comm.protocol import Error\n\n    body: dict[str, Any] = request.body or {}\n    kind: str | None = body.get(\"kind\")\n\n    if not kind:\n        return Error.to(\n            request,\n            code=\"unknown_kind\",\n            message=\"Request body must contain a 'kind' key (monitor, \"\n            \"config-get, or config-set).\",\n        )\n\n    handler = _KIND_DISPATCH.get(kind)\n    if handler is None:\n        return Error.to(\n            request,\n            code=\"unknown_kind\",\n            message=f\"Unknown management kind: {kind!r}. \"\n            f\"Supported kinds: {', '.join(COMPONENT_KINDS)}\",\n        )\n\n    return handler(self, request)\n</code></pre>"},{"location":"reference/brokered_entrypoint/#robotsix_calendar_agent.brokered_entrypoint.ComponentAgentResponder.set_agent","title":"<code>set_agent(agent: CalendarAgent) -&gt; None</code>","text":"<p>Bind the responder to a live :class:<code>CalendarAgent</code> instance.</p> <p>Called by :class:<code>CalendarAgent.__init__</code> after the responder is constructed, resolving the chicken-and-egg between the responder (needs agent for telemetry) and the agent (needs responder for dispatch).</p> Source code in <code>src/robotsix_calendar_agent/brokered_entrypoint.py</code> <pre><code>def set_agent(self, agent: CalendarAgent) -&gt; None:\n    \"\"\"Bind the responder to a live :class:`CalendarAgent` instance.\n\n    Called by :class:`CalendarAgent.__init__` after the responder\n    is constructed, resolving the chicken-and-egg between the\n    responder (needs agent for telemetry) and the agent (needs\n    responder for dispatch).\n    \"\"\"\n    self._agent = agent\n</code></pre>"},{"location":"reference/brokered_entrypoint/#robotsix_calendar_agent.brokered_entrypoint.main","title":"<code>main() -&gt; None</code>","text":"<p>Run the calendar agent as a long-lived service.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>CALENDAR_AGENT_TRANSPORT</code> holds an unrecognised value or a required brokered env var is missing.</p> Source code in <code>src/robotsix_calendar_agent/brokered_entrypoint.py</code> <pre><code>def main() -&gt; None:\n    \"\"\"Run the calendar agent as a long-lived service.\n\n    Raises:\n        ValueError: If ``CALENDAR_AGENT_TRANSPORT`` holds an unrecognised value\n            or a required brokered env var is missing.\n    \"\"\"\n    from .settings import Settings\n\n    settings = Settings()\n    mode = settings.CALENDAR_AGENT_TRANSPORT\n\n    # -- optional component-agent responder --------------------------------\n    component_responder = _build_component_responder(settings)\n\n    if mode == \"brokered\":\n        brokered = _build_brokered_agent()\n        # CalendarAgent wires its request handler onto the shared client.\n        CalendarAgent(\n            brokered.agent_id,\n            agent=brokered,\n            component_responder=component_responder,\n        )\n        # BrokeredAgent.serve_forever() installs signal handlers, starts the\n        # mailbox/pull loop, and blocks until SIGTERM/SIGINT.\n        brokered.serve_forever()\n        return\n\n    if mode in (\"\", \"inprocess\"):\n        logger.info(\"Using in-process transport\")\n        _serve_blocking(CalendarAgent(component_responder=component_responder))\n        return\n\n    _invalid_msg = (\n        f\"Invalid CALENDAR_AGENT_TRANSPORT={mode!r}; \"\n        \"expected 'inprocess' or 'brokered'.\"\n    )\n    raise ValueError(_invalid_msg)\n</code></pre>"},{"location":"reference/caldav_client/","title":"CalDAV Client","text":""},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client","title":"<code>robotsix_calendar_agent.caldav_client</code>","text":"<p>CalDAV/CardDAV client wrapper for Radicale.</p> <p>Typed, self-contained module wrapping the <code>caldav</code> library. All caldav-specific exceptions are converted to :class:<code>OperationError</code>.</p>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient","title":"<code>CalDavClient(url: str, username: str, password: str, default_calendar: str = '')</code>","text":"<p>Typed wrapper around <code>caldav.DAVClient</code> for Radicale.</p> <p>All operations convert caldav/requests exceptions to :class:<code>OperationError</code> with appropriate codes.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>Radicale server URL.</p> required <code>username</code> <code>str</code> <p>Radicale username.</p> required <code>password</code> <code>str</code> <p>Radicale password.</p> required <p>Raises:</p> Type Description <code>OperationError</code> <p>If authentication fails.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>def __init__(\n    self, url: str, username: str, password: str, default_calendar: str = \"\"\n) -&gt; None:\n    # ``caldav`` ships incomplete type information (mypy resolves the\n    # module to ``object``), so route it through ``Any`` to keep the\n    # wrapper strict-clean without per-call ignores.\n    import caldav\n\n    _caldav: Any = caldav\n\n    self._url = url\n    self._default_calendar = default_calendar\n    try:\n        self._client = _caldav.DAVClient(\n            url=url, username=username, password=password\n        )\n        self._principal = self._client.principal()\n        logger.info(\"CalDavClient connected to %s as %s\", url, username)\n    except _caldav.error.AuthorizationError as exc:\n        logger.exception(\"CalDAV auth failed for %s as %s: %s\", url, username, exc)\n        raise OperationError(\n            code=\"auth_failed\",\n            message=f\"Authentication failed: {exc}\",\n        ) from exc\n    except Exception as exc:\n        logger.exception(\"Failed to connect to Radicale at %s: %s\", url, exc)\n        raise OperationError(\n            code=\"caldav_error\",\n            message=f\"Failed to connect to Radicale: {exc}\",\n        ) from exc\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient.create_contact","title":"<code>create_contact(contact: Contact, addressbook_id: str = '') -&gt; Contact</code>","text":"<p>Create a contact; return the contact with server-assigned uid.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"create contact\")\ndef create_contact(self, contact: Contact, addressbook_id: str = \"\") -&gt; Contact:\n    \"\"\"Create a contact; return the contact with server-assigned uid.\"\"\"\n    logger.debug(\n        \"create_contact uid=%r addressbook_id=%r full_name=%r\",\n        contact.uid,\n        addressbook_id,\n        contact.full_name,\n    )\n    import uuid\n\n    if not contact.uid:\n        contact = Contact(\n            uid=str(uuid.uuid4()),\n            full_name=contact.full_name,\n            email=contact.email,\n            phone=contact.phone,\n            address=contact.address,\n            addressbook_id=contact.addressbook_id,\n        )\n    ab = self._get_addressbook(addressbook_id)\n    vcard = self._contact_to_vcard(contact)\n    saved = ab.save_object(vcard)\n    return self._to_contact(saved, addressbook_id=ab.name)\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient.create_event","title":"<code>create_event(event: CalendarEvent, calendar_id: str = '') -&gt; CalendarEvent</code>","text":"<p>Create an event; return the event with its server-assigned uid.</p> <p>If calendar_id is empty, use the default calendar.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"create event\")\ndef create_event(\n    self, event: CalendarEvent, calendar_id: str = \"\"\n) -&gt; CalendarEvent:\n    \"\"\"Create an event; return the event with its server-assigned uid.\n\n    If *calendar_id* is empty, use the default calendar.\n    \"\"\"\n    logger.debug(\n        \"create_event uid=%r calendar_id=%r summary=%r\",\n        event.uid,\n        calendar_id,\n        event.summary,\n    )\n    import uuid\n\n    if not event.uid:\n        # Generate a temporary UID; the server may replace it.\n        event = CalendarEvent(\n            uid=str(uuid.uuid4()),\n            summary=event.summary,\n            description=event.description,\n            location=event.location,\n            dtstart=event.dtstart,\n            dtend=event.dtend,\n            calendar_id=event.calendar_id,\n        )\n    cal = self._get_calendar(calendar_id)\n    ical = self._event_to_ical(event)\n    saved = cal.save_event(ical)\n    return self._to_calendar_event(saved, calendar_id=cal.name)\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient.delete_contact","title":"<code>delete_contact(uid: str, addressbook_id: str = '') -&gt; None</code>","text":"<p>Delete the contact identified by uid. Idempotent.</p> <p>Returns <code>None</code> when the UID does not exist (already deleted).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"delete contact\")\ndef delete_contact(self, uid: str, addressbook_id: str = \"\") -&gt; None:\n    \"\"\"Delete the contact identified by *uid*. Idempotent.\n\n    Returns ``None`` when the UID does not exist (already deleted).\n    \"\"\"\n    logger.debug(\"delete_contact uid=%r addressbook_id=%r\", uid, addressbook_id)\n    ab = self._get_addressbook(addressbook_id)\n    existing = ab.search(f\"UID:{uid}\")\n    if not existing:\n        return None\n    existing[0].delete()\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient.delete_event","title":"<code>delete_event(uid: str, calendar_id: str = '') -&gt; None</code>","text":"<p>Delete the event identified by uid. Idempotent.</p> <p>When calendar_id is empty, iterates all calendars to locate the UID.  Returns <code>None</code> when the UID does not exist in any calendar (already deleted).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"delete event\")\ndef delete_event(self, uid: str, calendar_id: str = \"\") -&gt; None:\n    \"\"\"Delete the event identified by *uid*. Idempotent.\n\n    When *calendar_id* is empty, iterates **all** calendars to locate\n    the UID.  Returns ``None`` when the UID does not exist in any\n    calendar (already deleted).\n    \"\"\"\n    logger.debug(\"delete_event uid=%r calendar_id=%r\", uid, calendar_id)\n    if calendar_id:\n        cal = self._get_calendar(calendar_id)\n        event_obj = cal.event(uid=uid)\n        if event_obj is None:\n            return None\n        event_obj.delete()\n    else:\n        for cal in self._iter_calendars(\"\"):\n            event_obj = cal.event(uid=uid)\n            if event_obj is not None:\n                event_obj.delete()\n                return None\n        return None\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient.health","title":"<code>health() -&gt; dict[str, Any]</code>","text":"<p>Perform a one-shot CalDAV reachability check.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p><code>{\"connected\": True, \"calendar_count\": int}</code> on success, or</p> <code>dict[str, Any]</code> <p><code>{\"connected\": False, \"error\": str}</code> when the probe fails.</p> <p>This is called on-demand by <code>monitor</code> \u2014 it does not cache stale state.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>def health(self) -&gt; dict[str, Any]:\n    \"\"\"Perform a one-shot CalDAV reachability check.\n\n    Returns:\n        ``{\"connected\": True, \"calendar_count\": int}`` on success, or\n        ``{\"connected\": False, \"error\": str}`` when the probe fails.\n\n    This is called on-demand by ``monitor`` \u2014 it does **not** cache\n    stale state.\n    \"\"\"\n    try:\n        calendars = self._principal.calendars()\n    except Exception as exc:\n        logger.warning(\"CalDAV health probe failed: %s\", exc)\n        return {\"connected\": False, \"error\": str(exc)}\n    else:\n        return {\"connected\": True, \"calendar_count\": len(calendars)}\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient.list_calendars","title":"<code>list_calendars() -&gt; list[str]</code>","text":"<p>Return the names of all available calendar collections.</p> <p>Only VEVENT / calendar collections are included (addressbooks come from the separate <code>self._principal.addressbooks()</code> and are inherently excluded).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list calendars\")\ndef list_calendars(self) -&gt; list[str]:\n    \"\"\"Return the names of all available calendar collections.\n\n    Only VEVENT / calendar collections are included (addressbooks\n    come from the separate ``self._principal.addressbooks()`` and\n    are inherently excluded).\n    \"\"\"\n    return [cal.name for cal in self._principal.calendars()]\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient.list_contacts","title":"<code>list_contacts(addressbook_id: str = '') -&gt; list[Contact]</code>","text":"<p>Return all contacts.</p> <p>If addressbook_id is empty, use the default address book.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list contacts\")\ndef list_contacts(self, addressbook_id: str = \"\") -&gt; list[Contact]:\n    \"\"\"Return all contacts.\n\n    If *addressbook_id* is empty, use the default address book.\n    \"\"\"\n    logger.debug(\"list_contacts addressbook_id=%r\", addressbook_id)\n    ab = self._get_addressbook(addressbook_id)\n    results = ab.search()\n    return [self._to_contact(r, addressbook_id=ab.name) for r in results]\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient.list_events","title":"<code>list_events(start: str, end: str, calendar_id: str = '') -&gt; list[CalendarEvent]</code>","text":"<p>Return events in the ISO 8601 date range.</p> <p>When calendar_id is empty, events are aggregated from all calendars.  Each event is tagged with its source <code>calendar_id</code>.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list events\")\ndef list_events(\n    self, start: str, end: str, calendar_id: str = \"\"\n) -&gt; list[CalendarEvent]:\n    \"\"\"Return events in the ISO 8601 date range.\n\n    When *calendar_id* is empty, events are aggregated from **all**\n    calendars.  Each event is tagged with its source ``calendar_id``.\n    \"\"\"\n    logger.debug(\n        \"list_events start=%r end=%r calendar_id=%r\",\n        start,\n        end,\n        calendar_id,\n    )\n    start_dt, end_dt = self._iso_date_range(start, end)\n    aggregated: list[CalendarEvent] = []\n    for cal in self._iter_calendars(calendar_id):\n        results = cal.search(\n            start=start_dt,\n            end=end_dt,\n            event=True,\n            expand=False,\n        )\n        aggregated.extend(\n            self._to_calendar_event(r, calendar_id=cal.name) for r in results\n        )\n    return aggregated\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient.list_tasks","title":"<code>list_tasks(calendar_id: str = '') -&gt; list[Task]</code>","text":"<p>Return all VTODO tasks from CalDAV calendar collections.</p> <p>When calendar_id is empty, tasks are aggregated from all calendars.  Each task is tagged with its source <code>calendar_id</code>.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"list tasks\")\ndef list_tasks(self, calendar_id: str = \"\") -&gt; list[Task]:\n    \"\"\"Return all VTODO tasks from CalDAV calendar collections.\n\n    When *calendar_id* is empty, tasks are aggregated from **all**\n    calendars.  Each task is tagged with its source ``calendar_id``.\n    \"\"\"\n    logger.debug(\"list_tasks calendar_id=%r\", calendar_id)\n    aggregated: list[Task] = []\n    for cal in self._iter_calendars(calendar_id):\n        results = cal.search(todo=True)\n        aggregated.extend(self._to_task(r, calendar_id=cal.name) for r in results)\n    return aggregated\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient.update_contact","title":"<code>update_contact(uid: str, contact: Contact, addressbook_id: str = '') -&gt; Contact</code>","text":"<p>Update the contact identified by uid; return the updated contact.</p> <p>Raises:</p> Type Description <code>OperationError</code> <p>If the UID doesn't exist (code <code>\"not_found\"</code>).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"update contact\")\ndef update_contact(\n    self, uid: str, contact: Contact, addressbook_id: str = \"\"\n) -&gt; Contact:\n    \"\"\"Update the contact identified by *uid*; return the updated contact.\n\n    Raises:\n        OperationError: If the UID doesn't exist (code ``\"not_found\"``).\n    \"\"\"\n    logger.debug(\n        \"update_contact uid=%r addressbook_id=%r full_name=%r\",\n        uid,\n        addressbook_id,\n        contact.full_name,\n    )\n    ab = self._get_addressbook(addressbook_id)\n    # Fetch to confirm existence \u2014 caldav addressbook search by UID\n    existing = ab.search(f\"UID:{uid}\")\n    if not existing:\n        raise OperationError(\n            code=\"not_found\",\n            message=f\"Contact with UID {uid!r} not found.\",\n        )\n    # Delete the old vcard and create a new one\n    existing[0].delete()\n    updated = Contact(\n        uid=uid,\n        full_name=contact.full_name,\n        email=contact.email,\n        phone=contact.phone,\n        address=contact.address,\n        addressbook_id=addressbook_id,\n    )\n    vcard = self._contact_to_vcard(updated)\n    saved = ab.save_object(vcard)\n    return self._to_contact(saved, addressbook_id=ab.name)\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalDavClient.update_event","title":"<code>update_event(uid: str, event: CalendarEvent, calendar_id: str = '') -&gt; CalendarEvent</code>","text":"<p>Update the event identified by uid; return the updated event.</p> <p>When calendar_id is empty, iterates all calendars to locate the UID (the UID may live in a non-default collection).  When calendar_id is given, only that single calendar is searched.</p> <p>Raises:</p> Type Description <code>OperationError</code> <p>If the UID doesn't exist (code <code>\"not_found\"</code>).</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>@_wrap_caldav_op(\"update event\")\ndef update_event(\n    self, uid: str, event: CalendarEvent, calendar_id: str = \"\"\n) -&gt; CalendarEvent:\n    \"\"\"Update the event identified by *uid*; return the updated event.\n\n    When *calendar_id* is empty, iterates **all** calendars to locate\n    the UID (the UID may live in a non-default collection).  When\n    *calendar_id* is given, only that single calendar is searched.\n\n    Raises:\n        OperationError: If the UID doesn't exist (code ``\"not_found\"``).\n    \"\"\"\n    logger.debug(\n        \"update_event uid=%r calendar_id=%r summary=%r\",\n        uid,\n        calendar_id,\n        event.summary,\n    )\n    if calendar_id:\n        cal = self._get_calendar(calendar_id)\n        existing = cal.event(uid=uid)\n        if existing is None:\n            raise OperationError(\n                code=\"not_found\",\n                message=f\"Event with UID {uid!r} not found.\",\n            )\n    else:\n        # Locate the UID across all calendars.\n        cal = None\n        for candidate in self._iter_calendars(\"\"):\n            if candidate.event(uid=uid) is not None:\n                cal = candidate\n                break\n        if cal is None:\n            raise OperationError(\n                code=\"not_found\",\n                message=f\"Event with UID {uid!r} not found.\",\n            )\n    # Build updated iCal with the same UID\n    updated = CalendarEvent(\n        uid=uid,\n        summary=event.summary,\n        description=event.description,\n        location=event.location,\n        dtstart=event.dtstart,\n        dtend=event.dtend,\n        calendar_id=calendar_id or cal.name,\n    )\n    ical = self._event_to_ical(updated)\n    saved = cal.save_event(ical)\n    return self._to_calendar_event(saved, calendar_id=cal.name)\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.CalendarEvent","title":"<code>CalendarEvent(*, uid: str = '', summary: str, description: str = '', location: str = '', dtstart: str, dtend: str, calendar_id: str = '')</code>  <code>dataclass</code>","text":"<p>Calendar event data transferred to/from the CalDAV server.</p> <p>Attributes:</p> Name Type Description <code>uid</code> <code>str</code> <p>Server-assigned unique identifier (empty for new events).</p> <code>summary</code> <code>str</code> <p>Short summary / title.</p> <code>description</code> <code>str</code> <p>Longer description (optional).</p> <code>location</code> <code>str</code> <p>Event location (optional).</p> <code>dtstart</code> <code>str</code> <p>Start time in ISO 8601 format.</p> <code>dtend</code> <code>str</code> <p>End time in ISO 8601 format.</p> <code>calendar_id</code> <code>str</code> <p>Calendar name the event belongs to (optional).</p>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.Contact","title":"<code>Contact(*, uid: str = '', full_name: str, email: str = '', phone: str = '', address: str = '', addressbook_id: str = '')</code>  <code>dataclass</code>","text":"<p>Contact data transferred to/from the CardDAV server.</p> <p>Attributes:</p> Name Type Description <code>uid</code> <code>str</code> <p>Server-assigned unique identifier (empty for new contacts).</p> <code>full_name</code> <code>str</code> <p>Display name.</p> <code>email</code> <code>str</code> <p>Email address (optional).</p> <code>phone</code> <code>str</code> <p>Phone number (optional).</p> <code>address</code> <code>str</code> <p>Postal address (optional).</p> <code>addressbook_id</code> <code>str</code> <p>Address book name the contact belongs to (optional).</p>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.OperationError","title":"<code>OperationError(code: str, message: str)</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a CalDAV/CardDAV operation fails.</p> <p>Attributes:</p> Name Type Description <code>code</code> <p>Machine-readable error code (e.g. <code>\"not_found\"</code>, <code>\"auth_failed\"</code>, <code>\"caldav_error\"</code>).</p> <code>message</code> <p>Human-readable error description.</p> Source code in <code>src/robotsix_calendar_agent/caldav_client.py</code> <pre><code>def __init__(self, code: str, message: str) -&gt; None:\n    super().__init__(message)\n    self.code = code\n    self.message = message\n</code></pre>"},{"location":"reference/caldav_client/#robotsix_calendar_agent.caldav_client.Task","title":"<code>Task(*, uid: str = '', summary: str, description: str = '', dtstart: str = '', due: str = '', status: str = '', calendar_id: str = '')</code>  <code>dataclass</code>","text":"<p>Represents a single VTODO task from a CalDAV calendar.</p> <p>Attributes:</p> Name Type Description <code>uid</code> <code>str</code> <p>Server-assigned unique identifier (empty for new tasks).</p> <code>summary</code> <code>str</code> <p>Short summary / title.</p> <code>description</code> <code>str</code> <p>Longer description (optional).</p> <code>dtstart</code> <code>str</code> <p>When the task starts (optional, ISO 8601).</p> <code>due</code> <code>str</code> <p>Task due date/time (VTODO DUE field, optional, ISO 8601).</p> <code>status</code> <code>str</code> <p>Task status \u2014 e.g. NEEDS-ACTION, IN-PROCESS, COMPLETED, CANCELLED.</p> <code>calendar_id</code> <code>str</code> <p>Calendar name the task belongs to (optional).</p>"},{"location":"reference/component_agent/","title":"Component Agent","text":"<p>The <code>component_agent</code> package embeds the <code>robotsix-agent-comm</code> responder template into the calendar agent, making it a fully manageable component.</p>"},{"location":"reference/component_agent/#overview","title":"Overview","text":"<ul> <li><code>ComponentAgentResponder</code> (<code>brokered_entrypoint.py</code>) \u2014 dispatches   <code>monitor</code>, <code>config-get</code>, and <code>config-set</code> request kinds.</li> <li><code>ConfigContract</code> (<code>config_contract.py</code>) \u2014 validates and applies   config updates with redaction, audit, and live-apply.</li> <li><code>ComponentAgentSettings</code> (<code>settings.py</code>) \u2014 gates the responder   (disabled by default, token-required-when-enabled).</li> </ul> <p>When enabled, the responder composes onto the existing <code>BrokeredAgent</code> connection (no second agent-id registration).</p>"},{"location":"reference/component_agent/#request-kinds","title":"Request Kinds","text":""},{"location":"reference/component_agent/#monitor","title":"<code>monitor</code>","text":"<p>Returns genuine live telemetry \u2014 real counters, uptime, and a CalDAV health probe.</p>"},{"location":"reference/component_agent/#config-get","title":"<code>config-get</code>","text":"<p>Returns a redacted config snapshot and per-key descriptors.</p>"},{"location":"reference/component_agent/#config-set","title":"<code>config-set</code>","text":"<p>Validates-then-applies config updates. Returns an audit map on success or a broker <code>Error</code> on invalid input.</p>"},{"location":"reference/component_agent/#configuration","title":"Configuration","text":"<p>See the Configuration guide for environment variables and settable keys.</p>"},{"location":"reference/component_agent/#api-reference","title":"API Reference","text":""},{"location":"reference/component_agent/#robotsix_calendar_agent.brokered_entrypoint.ComponentAgentResponder","title":"<code>robotsix_calendar_agent.brokered_entrypoint.ComponentAgentResponder(agent: CalendarAgent | None, settings: Any)</code>","text":"<p>Dispatch management-kind requests to live telemetry / config.</p> <p>Holds references to the running agent (for telemetry) and the live settings object (for config read/write).  It is transport-agnostic: it receives a <code>request</code> object and returns a protocol message; the caller owns the broker connection.</p> <p>Parameters:</p> Name Type Description Default <code>agent</code> <code>CalendarAgent | None</code> <p>The running :class:<code>CalendarAgent</code> instance.</p> required <code>settings</code> <code>Any</code> <p>The live :class:<code>~robotsix_calendar_agent.settings.Settings</code> instance the agent was built with.</p> required Source code in <code>src/robotsix_calendar_agent/brokered_entrypoint.py</code> <pre><code>def __init__(self, agent: CalendarAgent | None, settings: Any) -&gt; None:\n    self._agent = agent\n    self._settings = settings\n</code></pre>"},{"location":"reference/component_agent/#robotsix_calendar_agent.brokered_entrypoint.ComponentAgentResponder.on_request","title":"<code>on_request(request: Any) -&gt; Any</code>","text":"<p>Handle an incoming management-kind request.</p> <p>Dispatches on <code>request.body.get(\"kind\")</code>.  Unknown or missing kinds receive a broker <code>Error</code>.</p> Source code in <code>src/robotsix_calendar_agent/brokered_entrypoint.py</code> <pre><code>def on_request(self, request: Any) -&gt; Any:\n    \"\"\"Handle an incoming management-kind request.\n\n    Dispatches on ``request.body.get(\"kind\")``.  Unknown or missing\n    kinds receive a broker ``Error``.\n    \"\"\"\n    from robotsix_agent_comm.protocol import Error\n\n    body: dict[str, Any] = request.body or {}\n    kind: str | None = body.get(\"kind\")\n\n    if not kind:\n        return Error.to(\n            request,\n            code=\"unknown_kind\",\n            message=\"Request body must contain a 'kind' key (monitor, \"\n            \"config-get, or config-set).\",\n        )\n\n    handler = _KIND_DISPATCH.get(kind)\n    if handler is None:\n        return Error.to(\n            request,\n            code=\"unknown_kind\",\n            message=f\"Unknown management kind: {kind!r}. \"\n            f\"Supported kinds: {', '.join(COMPONENT_KINDS)}\",\n        )\n\n    return handler(self, request)\n</code></pre>"},{"location":"reference/component_agent/#robotsix_calendar_agent.component_agent.config_contract","title":"<code>robotsix_calendar_agent.component_agent.config_contract</code>","text":"<p>Config contract for the component-agent responder.</p> <p>Provides validation, snapshot, and live-apply for the running :class:<code>Settings</code> object, following the <code>robotsix-agent-comm</code> component-agent pattern.</p> <p>Dotted-path key mapping</p> <p>Config keys are derived from <code>Settings</code> field names by lowercasing: <code>RADICALE_DEFAULT_CALENDAR</code> \u2192 <code>radicale_default_calendar</code>. Secret fields are redacted in all read paths using the <code>\"***\"</code> sentinel.</p>"},{"location":"reference/component_agent/#robotsix_calendar_agent.component_agent.config_contract.SETTABLE_KEYS","title":"<code>SETTABLE_KEYS: frozenset[str] = frozenset({'radicale_default_calendar'})</code>  <code>module-attribute</code>","text":""},{"location":"reference/component_agent/#robotsix_calendar_agent.component_agent.config_contract.ConfigContractError","title":"<code>ConfigContractError(code: str, message: str, details: Any = None)</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a config operation fails validation.</p> <p>Mirrors the shape of <code>robotsix_agent_comm.protocol.Error</code> so the responder can translate it cleanly.</p> Source code in <code>src/robotsix_calendar_agent/component_agent/config_contract.py</code> <pre><code>def __init__(self, code: str, message: str, details: Any = None) -&gt; None:\n    super().__init__(message)\n    self.code = code\n    self.message = message\n    self.details = details\n</code></pre>"},{"location":"reference/component_agent/#robotsix_calendar_agent.component_agent.config_contract.get_config_snapshot","title":"<code>get_config_snapshot(settings: Any, comp_settings: Any | None = None) -&gt; dict[str, Any]</code>","text":"<p>Return a flat, dotted-path view of current config with secrets redacted.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>Any</code> <p>The live core :class:<code>Settings</code> instance.</p> required <code>comp_settings</code> <code>Any | None</code> <p>The live :class:<code>ComponentAgentSettings</code> instance (if available).</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A <code>{key: value}</code> dict where keys are lowercased field names</p> <code>dict[str, Any]</code> <p>and secret values are replaced with <code>\"***\"</code>.</p> Source code in <code>src/robotsix_calendar_agent/component_agent/config_contract.py</code> <pre><code>def get_config_snapshot(\n    settings: Any,\n    comp_settings: Any | None = None,\n) -&gt; dict[str, Any]:\n    \"\"\"Return a flat, dotted-path view of current config with secrets redacted.\n\n    Args:\n        settings: The live core :class:`Settings` instance.\n        comp_settings: The live :class:`ComponentAgentSettings` instance\n            (if available).\n\n    Returns:\n        A ``{key: value}`` dict where keys are lowercased field names\n        and secret values are replaced with ``\"***\"``.\n    \"\"\"\n    if comp_settings is None:\n        from .settings import ComponentAgentSettings\n\n        comp_settings = ComponentAgentSettings()\n\n    result: dict[str, Any] = {}\n    for key, value in _iter_config_fields(settings, comp_settings):\n        if _is_secret_field(key) or hasattr(value, \"get_secret_value\"):\n            result[key] = _REDACTED\n        else:\n            result[key] = value\n\n    return result\n</code></pre>"},{"location":"reference/component_agent/#robotsix_calendar_agent.component_agent.config_contract.describe_config","title":"<code>describe_config(settings: Any, comp_settings: Any | None = None) -&gt; dict[str, Any]</code>","text":"<p>Return per-key descriptors for every config key.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>``{\"keys\": {key: {\"type\": ..., \"settable\": bool, \"secret\": bool,</p> <code>dict[str, Any]</code> <p>\"value\": ...}}}``</p> Source code in <code>src/robotsix_calendar_agent/component_agent/config_contract.py</code> <pre><code>def describe_config(\n    settings: Any,\n    comp_settings: Any | None = None,\n) -&gt; dict[str, Any]:\n    \"\"\"Return per-key descriptors for every config key.\n\n    Returns:\n        ``{\"keys\": {key: {\"type\": ..., \"settable\": bool, \"secret\": bool,\n        \"value\": ...}}}``\n    \"\"\"\n    if comp_settings is None:\n        from .settings import ComponentAgentSettings\n\n        comp_settings = ComponentAgentSettings()\n\n    keys: dict[str, dict[str, Any]] = {}\n    for key, value in _iter_config_fields(settings, comp_settings):\n        secret = _is_secret_field(key) or hasattr(value, \"get_secret_value\")\n        keys[key] = {\n            \"type\": type(value).__name__,\n            \"settable\": key in SETTABLE_KEYS,\n            \"secret\": secret,\n            \"value\": _REDACTED if secret else value,\n        }\n\n    return {\"keys\": keys}\n</code></pre>"},{"location":"reference/component_agent/#robotsix_calendar_agent.component_agent.config_contract.validate_config_update","title":"<code>validate_config_update(settings: Any, updates: dict[str, Any]) -&gt; None</code>","text":"<p>Validate updates without applying them.</p> <p>Rejects unknown keys, keys not in :data:<code>SETTABLE_KEYS</code>, and values that would fail pydantic validation when combined with the current live config.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>Any</code> <p>The live core :class:<code>Settings</code> instance.</p> required <code>updates</code> <code>dict[str, Any]</code> <p><code>{dotted_key: new_value}</code> dict.</p> required <p>Raises:</p> Type Description <code>ConfigContractError</code> <p>If any validation rule is violated.</p> Source code in <code>src/robotsix_calendar_agent/component_agent/config_contract.py</code> <pre><code>def validate_config_update(\n    settings: Any,\n    updates: dict[str, Any],\n) -&gt; None:\n    \"\"\"Validate *updates* without applying them.\n\n    Rejects unknown keys, keys not in :data:`SETTABLE_KEYS`, and values\n    that would fail pydantic validation when combined with the current\n    live config.\n\n    Args:\n        settings: The live core :class:`Settings` instance.\n        updates: ``{dotted_key: new_value}`` dict.\n\n    Raises:\n        ConfigContractError: If any validation rule is violated.\n    \"\"\"\n    from ..settings import Settings as CoreSettings\n\n    # 1. Reject unknown / non-settable keys\n    for key in updates:\n        field_name = _resolve_field_name(key)\n        if field_name not in CoreSettings.model_fields:\n            raise ConfigContractError(\n                code=\"unknown_key\",\n                message=f\"Unknown config key: {key!r}\",\n                details={\"key\": key},\n            )\n        if key not in SETTABLE_KEYS:\n            raise ConfigContractError(\n                code=\"not_settable\",\n                message=f\"Config key {key!r} is not settable at runtime\",\n                details={\"key\": key},\n            )\n\n    if not updates:\n        return\n\n    # 2. Build a candidate Settings object from current real values + updates\n    candidate_kwargs: dict[str, Any] = {}\n    for name in CoreSettings.model_fields:\n        candidate_kwargs[name] = getattr(settings, name)\n    for key, new_val in updates.items():\n        field_name = _resolve_field_name(key)\n        candidate_kwargs[field_name] = new_val\n\n    try:\n        CoreSettings(**candidate_kwargs)\n    except PydanticValidationError as exc:\n        raise ConfigContractError(\n            code=\"invalid_value\",\n            message=f\"Config validation failed: {exc}\",\n            details={\"errors\": exc.errors()},\n        ) from exc\n</code></pre>"},{"location":"reference/component_agent/#robotsix_calendar_agent.component_agent.config_contract.apply_config_update","title":"<code>apply_config_update(settings: Any, updates: dict[str, Any]) -&gt; dict[str, tuple[Any, Any]]</code>","text":"<p>Validate updates then apply them to the live settings.</p> <p>Returns an audit map <code>{key: (old_value, new_value)}</code> with secret values redacted.</p> <p>Raises:</p> Type Description <code>ConfigContractError</code> <p>If validation fails \u2014 no mutation occurs.</p> Source code in <code>src/robotsix_calendar_agent/component_agent/config_contract.py</code> <pre><code>def apply_config_update(\n    settings: Any,\n    updates: dict[str, Any],\n) -&gt; dict[str, tuple[Any, Any]]:\n    \"\"\"Validate *updates* then apply them to the live *settings*.\n\n    Returns an **audit map** ``{key: (old_value, new_value)}`` with\n    secret values redacted.\n\n    Raises:\n        ConfigContractError: If validation fails \u2014 no mutation occurs.\n    \"\"\"\n    # Validate first \u2014 never mutate on invalid.\n    validate_config_update(settings, updates)\n\n    if not updates:\n        return {}\n\n    audit: dict[str, tuple[Any, Any]] = {}\n    for key, new_val in updates.items():\n        field_name = _resolve_field_name(key)\n        old_val = getattr(settings, field_name)\n        # Redact secret values in the audit trail\n        old_redacted = _REDACTED if _is_secret_field(key) else old_val\n        new_redacted = _REDACTED if _is_secret_field(key) else new_val\n        setattr(settings, field_name, new_val)\n        audit[key] = (old_redacted, new_redacted)\n\n    # Emit audit log\n    logger.info(\n        \"config-set applied %d change(s): %s\",\n        len(audit),\n        {k: (old, new) for k, (old, new) in audit.items()},\n    )\n\n    return audit\n</code></pre>"},{"location":"reference/component_agent/#robotsix_calendar_agent.component_agent.settings.ComponentAgentSettings","title":"<code>robotsix_calendar_agent.component_agent.settings.ComponentAgentSettings</code>","text":"<p>               Bases: <code>BaseSettings</code></p> <p>Settings that gate and configure the component-agent responder.</p> <p>All fields follow the existing naming convention (uppercase, <code>COMPONENT_AGENT_</code> prefix).</p> <p>The token-required-when-enabled invariant is enforced at construction time so misconfiguration is caught early.</p>"},{"location":"reference/intent_parser/","title":"Intent Parser","text":""},{"location":"reference/intent_parser/#robotsix_calendar_agent.intent_parser","title":"<code>robotsix_calendar_agent.intent_parser</code>","text":"<p>llmio-based intent parser for calendar/contact instructions.</p> <p>Converts natural-language text into a structured :class:<code>ParsedIntent</code> with one of 8 operation types and extracted parameters.</p>"},{"location":"reference/intent_parser/#robotsix_calendar_agent.intent_parser.CalendarOperation","title":"<code>CalendarOperation</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Calendar (CalDAV) operation types.</p>"},{"location":"reference/intent_parser/#robotsix_calendar_agent.intent_parser.ContactOperation","title":"<code>ContactOperation</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Contacts (CardDAV) operation types.</p>"},{"location":"reference/intent_parser/#robotsix_calendar_agent.intent_parser.IntentParseError","title":"<code>IntentParseError(message: str)</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when intent parsing fails (llmio error or unclassifiable input).</p> Source code in <code>src/robotsix_calendar_agent/intent_parser.py</code> <pre><code>def __init__(self, message: str) -&gt; None:\n    super().__init__(message)\n</code></pre>"},{"location":"reference/intent_parser/#robotsix_calendar_agent.intent_parser.IntentParser","title":"<code>IntentParser(model_config: dict[str, Any] | None = None)</code>","text":"<p>Parser that uses <code>robotsix-llmio</code> to classify instructions.</p> <p>Parameters:</p> Name Type Description Default <code>model_config</code> <code>dict[str, Any] | None</code> <p>Forwarded to the llmio provider for model selection/temperature/etc.  If <code>None</code>, llmio defaults apply.</p> <code>None</code> Source code in <code>src/robotsix_calendar_agent/intent_parser.py</code> <pre><code>def __init__(self, model_config: dict[str, Any] | None = None) -&gt; None:\n    self._model_config = model_config or {}\n</code></pre>"},{"location":"reference/intent_parser/#robotsix_calendar_agent.intent_parser.IntentParser.parse","title":"<code>parse(text: str) -&gt; ParsedIntent</code>","text":"<p>Parse natural-language text into a :class:<code>ParsedIntent</code>.</p> <p>Raises:</p> Type Description <code>IntentParseError</code> <p>If the llmio call fails or the model cannot produce a valid structured result.</p> Source code in <code>src/robotsix_calendar_agent/intent_parser.py</code> <pre><code>def parse(self, text: str) -&gt; ParsedIntent:\n    \"\"\"Parse natural-language *text* into a :class:`ParsedIntent`.\n\n    Raises:\n        IntentParseError: If the llmio call fails or the model cannot\n            produce a valid structured result.\n    \"\"\"\n    try:\n        from robotsix_llmio.core import build_agent_for_level, run_agent\n\n        # level=2 is the standard LLMIO default (DeepSeek V4 Pro).\n        # The implement agent already uses this tier successfully for\n        # tool calls and structured output via pydantic-ai.\n        handle = build_agent_for_level(\n            2,\n            provider_kwargs=self._model_config or None,\n            system_prompt=_INTENT_SYSTEM_PROMPT,\n            output_type=_IntentOutput,\n        )\n\n        def _run() -&gt; _IntentOutput:\n            # AgentHandle delegates attribute access to the wrapped\n            # pydantic-ai Agent, so call run_sync on the handle directly.\n            # pydantic-ai &gt;=1.0 exposes the result on ``.output``.\n            result = handle.run_sync(text)\n            return result.output  # type: ignore[no-any-return]\n\n        output = run_agent(\n            handle,\n            _run,\n            label=\"intent-parse\",\n            what=\"intent parsing\",\n        )\n        if not isinstance(output, _IntentOutput):\n            _msg = f\"Unexpected output type from llmio: {type(output)}\"\n            raise IntentParseError(_msg)\n\n        result = ParsedIntent(\n            operation=output.operation,  # type: ignore[arg-type]\n            params=output.params or {},\n            original_text=text,\n        )\n        logger.info(\"Parsed intent: operation=%r text=%r\", result.operation, text)\n        return result\n    except IntentParseError as exc:\n        logger.exception(\"Intent parse error for '%s': %s\", text, exc)\n        raise\n    except Exception as exc:\n        logger.exception(\n            \"Unexpected error during intent parsing for '%s': %s\", text, exc\n        )\n        _msg = f\"Intent parsing failed: {exc}\"\n        raise IntentParseError(_msg) from exc\n</code></pre>"},{"location":"reference/intent_parser/#robotsix_calendar_agent.intent_parser.ParsedIntent","title":"<code>ParsedIntent(*, operation: CalendarOperation | ContactOperation | TaskOperation, params: dict[str, Any] = dict(), original_text: str = '')</code>  <code>dataclass</code>","text":"<p>Result of parsing a user instruction.</p> <p>Attributes:</p> Name Type Description <code>operation</code> <code>CalendarOperation | ContactOperation | TaskOperation</code> <p>The classified operation.</p> <code>params</code> <code>dict[str, Any]</code> <p>Operation-specific arguments (dates, uids, fields, ...).</p> <code>original_text</code> <code>str</code> <p>The raw instruction string, preserved for logging.</p>"},{"location":"reference/intent_parser/#robotsix_calendar_agent.intent_parser.TaskOperation","title":"<code>TaskOperation</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Tasks (CalDAV VTODO) operation types.</p>"},{"location":"reference/settings/","title":"Settings","text":""},{"location":"reference/settings/#robotsix_calendar_agent.settings","title":"<code>robotsix_calendar_agent.settings</code>","text":"<p>Single source of truth for all environment-variable configuration.</p> <p>Uses :class:<code>pydantic_settings.BaseSettings</code> to read, validate, and normalise configuration from the process environment.</p>"},{"location":"reference/settings/#robotsix_calendar_agent.settings.Settings","title":"<code>Settings</code>","text":"<p>               Bases: <code>BaseSettings</code></p> <p>Application settings loaded from environment variables.</p> <p>All RADICALE_* and BROKER_HOST/BROKER_AGENT_TOKEN fields have empty defaults so that the existing constructor-argument fallback and emptiness checks in <code>CalendarAgent.__init__</code> and <code>_build_brokered_agent</code> continue to work unchanged.</p>"},{"location":"tutorials/","title":"Tutorials","text":"<p>See the Getting started section of the home page for a step-by-step walkthrough.</p> <p>More tutorials coming soon.</p>"}]}
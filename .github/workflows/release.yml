name: Release

on:
  push:
    tags: ["*"]

permissions:
  contents: read

jobs:
  release:
    permissions:
      id-token: write  # needed for PyPI trusted publishing
      contents: read
    uses: damien-robotsix/robotsix-github-workflows/.github/workflows/python-release.yml@77e10e286717cfe0eeb6b8d9ce985f3794236d15  # main
